# Tank-generation handoff — <ID/batch>

Use this for “continue” or a new agent. Link the durable run packet and replace
placeholders; don't paste a whole transcript. Revalidate the live state on resume.

## Resume here

- Latest owner request and superseded instructions:
- Run packet / batch contract:
- Exact next command or bounded editing action:
- Expected success condition and what to do if it fails:
- Required owner decision, if any:

## Workspace and ownership

- Worktree absolute path / branch / HEAD / upstream base:
- Dirty files owned by this task:
- Other agents' files / unrelated WIP not to touch:
- Active owner per profile / shared registry / critic:
- Current diff or geometry fingerprint:
- Source hashes and where the next machine can obtain permitted inputs:
- Installed dependencies / missing tools / comparison registration gaps:

## State, not optimism

- Implemented:
- Qualified (all required gates or explicit failures):
- Published (commit/remote hash, or not published):
- Frozen source frame / candidate revision:
- Last complete successful run and artifact:
- Failed checks with exact results:
- NOT RUN / interrupted / blocked checks:
- Stale results invalidated by subsequent changes:
- Target decisions already answered (names, proportions, donor recipe):
- Publication authority and exclusions; exceptions apply only to named scope:

## Verification and continuation

- Required view list and missing images:
- Remaining geometry/contact/negative-space issues, worst first:
- Remaining gameplay/ERA/track/asset issues:
- Open fleet-style/performance issue IDs; measured costs versus unverified causes:
- Per-ID roller, track-thickness, chassis-closure and material-role coverage:
- Cold/warm/rapid-switch benchmark state and frozen budgets:
- Original-model and unrelated-record preservation evidence:
- Local artifact paths plus regeneration commands if ignored/machine-local:
- Experiments rejected and why (do not redo without new evidence):
- Next checkpoints and final full release command:

## Process hygiene

- Owned server/browser/test PIDs, worktrees, ports and queue tickets:
- Which preview the user is actively using:
- Which processes were stopped / intentionally left running and why:
- No permission to kill other worktrees' processes or bypass their queue lock.
