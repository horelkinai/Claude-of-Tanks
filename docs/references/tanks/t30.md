# T30 Heavy Tank (`t30`) — reference packet

**Exact variant modeled:** Heavy Tank T30 (1945-48 trials), 155 mm T7 gun —
the T29-family hull with the bigger gun and cast turret.

## Corroborated dimensions (spec-published, specs.ts)

| Measure | Value | Source |
|---|---|---|
| Hull length | 7.6 m | specs.ts dims (T29/T30 family data) |
| Overall length | 10.9 m (155 mm ~3.2 m past the bow) | specs.ts dims |
| Width | 3.8 m (trackWidthM 0.71) | specs.ts dims/visual |
| Height | 3.25 m | specs.ts dims |
| Running gear | 8 dual road wheels/side, 4 return rollers, REAR drive sprocket, front idler | T29-family references |

## Local GLB oracle status — GATE-BLOCKED (certified, ww2 r2)

`public/models/tanks/community/t30_haphazard.glb` is registered as
`candidateGlb` only ("kept for the repeatable A/B audit, but never
activated") with `qaReason: 'single fused mesh cannot articulate the real
T30 turret'`. Probed 2026-08-04: the GLB contains exactly ONE node
(`hull`, mesh `Object.113`). modelLoader's fixedMount guard (modelLoader.js
~L2244) exists expressly so this print can never load as a casemate on the
turreted t30 spec, and without a turret node the swap throws
`no articurable turret node`. Consequence: **no geometry-gate row is
currently possible** — the fidelity harness loads references through the
game loader. Unblocking is an ORCHESTRATOR-LANE decision: either a
measurement-only loader path for fused prints on turreted specs, or a
split repair of the print (hull/turret separation, warp law v2 lane). Do
NOT add a LOCAL_REFERENCE_OVERRIDE meanwhile — a failing override costs
every fleet gate run a 150 s timeout (verified, then reverted).

## ww2 r2 (2026-08-04): TRACK ANOMALY ROOT-CAUSED + standard rig built

The containment audit read `expected 2 band meshes, found 0 (no
ground-riding track buckets either)`. ROOT CAUSE: t30 was
`source:'procedural'` with NO profile builder anywhere — it fell through
`resolveBuilder` to tankFactory's `buildCommunityPlaceholder`, which
builds hull slabs plus two `hullRubber` track PONTOON BOXES: no factory
band meshes (DynamicDrawUsage), no `userData.trackBucket` geometry — the
audit (and the game's articulation/damage systems, §H) had nothing to
find.

FIX: bespoke `buildT30` in src/vehicles/profiles/ww2.ts (WW2_PROFILES.t30)
with the STANDARD §H base rig:
- `KIT.buildRunningGear` two-layer shoe system (pads + inner chain/guide
  horns riding 8 dual wheels/side, rear sprocket z −3.5, front idler
  z +3.55, 4 return rollers, trackW 0.71, factory band meshes) — the
  audit now runs in `bands` mode: **containment 0/0 exact voxels** (the
  new-build target; bow/tail plate lines seated above the wrap arcs, belly
  ±1.10 clear of the 1.145 lane inner faces).
- Hull loft to published dims (belly + sponson band ±1.84 + 54° glacis +
  engine-deck fall + tail), fenders ±1.90 (width anchor 3.8), turret ring
  at y 2.30 (spec turretPivot), cast dome + bustle, 155 mm with rounded
  mantlet collar and muzzle at overall 10.9.
- §B3: FITTINGS.pintleMG (m2, dark) parked over the bustle; census mg1.
  Decoration: lights+guards, tow cable, fender tools, spare links, hatches,
  louvres, star decals.
- tank-standard-check: clip 0/0 ✓, contiguity/holes 0 ✓, decor mg1 ✓
  (gate column reads "no gate json" — see the blocker above).
- shots/ww2-r2/after/t30.png (eyeballed: two-layer tracks read on real
  wheels, no floating bands, 155 mm overhang, no top-down holes).

NEXT: (1) orchestrator: pick the unblock lane (loader measurement path vs
print split repair) — any real gate score is progress from the zero row;
(2) first gate round will re-proportion the turret/glacis against the
print (this build is published-data-sovereign only); (3) visual round:
muzzle brake variant check (T30E1), stowage variety pass.
