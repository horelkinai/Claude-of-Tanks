# FV510 Warrior (`fv510`) — reference packet

Exact variant: FV510 Warrior section vehicle, 30 mm L21A1 RARDEN in the two-man steel
turret.

## Corroborated real dimensions
- Length 6.3 m (hull = overall; the RARDEN does not clear the nose); width 3.03 m;
  height 2.8 m to the turret roof.
  Sources: https://en.wikipedia.org/wiki/FV510_Warrior ,
  https://www.militaryfactory.com/armor/detail.php?armor_id=27 ,
  https://en.wikipedia.org/wiki/RARDEN
- Gun: 30 mm L21A1 RARDEN — thin bare 2.43 m barrel (3.15 m overall), NO evacuator, NO
  sleeve, distinctive slotted muzzle; mounted high in the turret face, usually carried
  with a few degrees of elevation.
- Running gear: 6 road wheels per side, FRONT drive sprocket, raised rear idler, top run
  behind aluminium skirts/appliqué strakes.
- Distinctive identity: tall troop hull with a long shallow glacis and near-vertical
  ribbed flanks (horizontal appliqué strakes), rear power door, big boxy two-man turret
  set forward-center with flat sloped face, twin sight heads, two whip antennas and the
  tall L-shaped exhaust/NBC stack on the right rear quarter.

## Local GLB oracle (recovered drop, articulated)
Width-normalized reference (scale ×1.071): hull z ±2.83 (5.66 — proportionally shorter
than real; the GLB's width bound is its protruding WING MIRRORS, whose tips define
spec-width normalization — its body is only ~88 % of 3.03 m, so a mirror-less procedural
renders ~13 % oversized everywhere; matching mirrors were added), hull top 2.30, turret
x −1.36..+1.47 with sight/mast gear reaching y 3.9; RARDEN pivot at (−0.55, 2.0, 1.29) —
offset LEFT of center; barrel never passes the nose (gun overhang legitimately empty on
both sides → gun component 100 for matching geometry).

**ORACLE DEFECT (turret component):** the GLB's `Turret` node (TurretMesh, post-yaw
z −0.78..+2.64, 3.4 m long) fuses the entire upper BOW/glacis plating with the turret,
and its `Gun` node is a 2 cm sliver. The fidelity "turret" mask therefore includes hull
bow mass no honest rotating turret can cover — turret component is structurally capped
near ~36 (an assembly/followers fix in src/vehicles/userdrops5.js, outside UK-family
ownership).

## Procedural gaps identified (baseline 72.3: H80 T34 G100 R72)
- Procedural hull too long (6.34 vs 5.66) and far too low (top 1.82 vs 2.30).
- Turret much too small: ±0.82 wide × 0.93 tall vs oracle ±1.4 × ~1.2 plus masts.
- No exhaust stack / sight heads; skirt band and 6-wheel line sat too low (tracks R72).

**Oracle re-processed (repair_oracles_blender.py): turret node purified** —
the bow/glacis plating and wing mirrors carved out of the fused TurretMesh
into a hull-side BowPlating node (mirrors keep the width bound, in place);
the Turret node now holds only the actual turret; Gun sliver kept (RARDEN
never clears the nose, gun-overhang masks stay empty on both sides). Note:
the rebuilt procedural turret was sized against the old fused mask, so the
honest oracle now reads it ~oversized (T holds ~36 until the family re-tunes).

## Mismatch log — shaded-parity r2 (2026-07-30)
- Post-repair oracle re-tune: the old oracle fused ~3.4 m of bow plating into the turret
  mask and the r1 turret was sized against it (~±1.31). Rebuilt as the real two-man SQUARE
  turret (±0.85, frustum box) — T 36 -> 45, total 71.9 -> 73.8.
- RARDEN: fat muzzle stub replaced by the thin r0.030 tube with stepped sleeve segments +
  dark flash hider; mantlet block + collar at the face; gun moved to the right of the
  sight pod (gunX +0.22). Tube tip 3.18 vs hull nose 3.19 — never clears the nose, so the
  gun-overhang score stays 100 by construction. Real tube ≈ 2 m from the face — kept at
  1.85 m reach to hold the mask (cap noted).
- Floating wing mirrors REMOVED (r1 "loose dark boxes at odd angles"): the ±1.72 width
  bound they provided is now a strapped fender stowage bin row on bracket bars.
- Hull now carries the reference's slat/bar-armour language: 5-bar bow bank hugging the
  glacis, 5-bar flank banks on hanger posts over the skirts, 4-bar rear bank, rear
  top-corner rail frames over the troop bay.
- Exhaust cowl moved to the LEFT hull side (Warrior signature; r1 had it right-rear) with
  outlet + heat-shield louvres; raised louvred powerpack bank on the right front deck;
  driver hatch ring + twin periscope hoods on the right glacis shoulder (per critique).
- Rear troop door: raised frame + hinge blocks + handle + low bin rack. Comms mast now on
  a base pot above the deck (was fully buried inside the hull). Turret: split vision
  blocks, sight pod + cap, 2x4 smoke banks per cheek, rear basket with mesh + bundles,
  whip antennas on pots, wire cutter.
- Residual: H ~78 — the ref hull mask includes its dense slat cages (thicker in silhouette
  than my bar banks); matching that fully would mean modelling the whole cage volume.

## Round-3 log — turret volume re-measure (2026-07-30)
- r2 turret mask 45.1: measured the REPAIRED oracle's TurretMesh directly (band profile,
  width-anchored): body walls are a COMPACT box — ~1.6 wide, ~1.26 deep, ~0.5 visible
  over the deck, centred at +0.5 of the hull (the r2 pivot +0.93 and 2.05-deep box were
  tuned against the old fused turret+bow mask; the shaded-pair "bigger ref turret" is a
  per-model-camera framing illusion — trust masks, not the pair).
- Turret rebuilt to the oracle envelope: h 0.85, tw 0.85, walls +0.78..-0.72, basket at
  -1.02, pivot z 0.93 -> 0.55; dischargers hung off the face corners (tubes never read as
  face studs); sight pod/hatches/antennas re-seated on the smaller roof.
- Headline 73.8 -> 78.6, T 45.1 -> 65 (biggest UK turret-component move this round).


## Gate v6/v7 iteration (2026-07-31)
Rebuilt to published dims: hull = overall 6.34 (the recovered oracle is ~10%
short — certified), width 3.03 with the slat-armour faces exactly on the
committed plane (the v5 fender bins breached to +-1.72 on a 3.03 vehicle —
the whole tank was silently shrunk ~12%, the single largest width-guard
breach in the family), turret lowered to the oracle's 2.47 roof with the
sight pod as the 2.80 p95 anchor, single mid mast (2 columns) instead of the
v5 twin 1.55 m whips, tracks/gear pulled inside the committed width and hull
span. dims 91.8 (heightM/width quantization residuals), floaters 100;
curve/station rows carry the certified 10%-short-oracle cap (~0-5).


## Gate v10 cap re-verification (2026-07-31)
The short-print cert STANDS under v10: the recovered oracle reads ~10%
short — curve rows structurally capped (hull/whole/turret 0, stations
~5); the build holds published dims (6.34/3.03/2.80) with the RARDEN
never clearing the nose. Dims green 100 (v10 pixel width); floaters 100.

## Plate-fill r1 (2026-08-01, owner directive)
Inherits the shared ukHull fender-wedge fill (deck falls below the 1.36
fender line at both hull ends; the wedges close under the plate). Gate v11
before/after byte-identical (its standing all-zero curve row is unrelated to
this fill — see the orientation/oracle notes). Evidence: shots/plate-fill-r1/
fv510-{before,after}/.

## PHOTO-CLASS FULL ROUND (2026-08-06, uk fv510 builder) — "made actual"
OWNER ORDER (verbatim): "i really want the fv510 to be made actual." Full
build-up round; before -> after proc hash c3b688fc -> 6bfcee8 (33 -> 64
meshes, 39,428 -> 59,784 verts). Graduates held byte-identical through the
round: chieftain5 94c09bb0, centurion3 fea56ecc (tmp-hashgeo before first
edit + at close). npm test green (166 + track-geometry).

### 1. Oracle adjudication (ORDERS #1 — the 0-row and the disabled recipe)
- **Reference state: MEASURABLE.** recovered/fv510.glb (sha1 9633823…, the
  946da9d round-3 turret-purified bytes; never warped in git) loads,
  registers (turretNode ^Turret$, gunNode ^Gun$, autoPivot, yawOffset PI,
  flip:true) and renders sanely — fresh vertex extract 2026-08-06 (counts
  5/29,817/28,582; loaderScale 211.302, safeScaleK 1.0712, overall 5.646 =
  **-10.9% uniform shortness**, width -0.3%). The ledger 0-row is an HONEST
  capped measurement, not a false-0: gate x1 adjudication reproduced
  0/0/0/0 + dims 100 + floaters 100 before any edit.
- **Row-0 mechanism DECODED** (first time with terms): the curve score is
  `100 - 12*meanPct - 0.6*p95Pct - 1.5*coverPct` (procedural-fidelity.html
  ~:1007; coverPct = single-sided-column share, LOW = good). Control run:
  side_hull mean 6.86%, p95 30.9%, cover 5.29% -> 12*6.86 alone eats the
  row. The -10.9% de-phase plus genuine print-shape divergence (print
  fore-deck tops 1.66-1.69 vs the real Warrior's ~1.93 driver plate; troop
  roof 2.07 both) hold mean ~7-9% for ANY published-dims photo-class build.
- **Disabled warp recipe ADJUDICATED — the "inert" read was a misread.**
  §E REQUEST-INTERCEPTION SIM (tools/tmp-fv510-warpsim.mjs; candidate bytes
  built in scratch from the parked literals via repair_oracles._axis_warp,
  census guard (5, 29817, 28582) passed; repo GLB untouched, sha verified):
  - control (committed bytes):  side_hull mean 6.86 / cover 5.29 / safeScale 1.0712
  - warped  (candidate bytes):  side_hull mean 8.67 / cover **1.12** / safeScale **0.9996**
  The warp does EXACTLY what it was planned to do: single-sided columns
  collapse 5.29 -> 1.12 (ref frame now published-length) and the width
  true-up lands 1.0. The headline stays 0 because the 12*mean term is
  dominated by real shape divergence — the incident judged "inert" from
  the 0-floored headline alone.
- **Why the warp needs its y-knee (loader law):** modelLoader normalization
  is `s = min(targetLen/size.z, width*1.08/size.x, height*1.30/size.y)`
  (modelLoader.js ~2341). The mast-heavy print binds on the HEIGHT clamp
  (3.64/0.0172 = 211.30 — exactly the extract's loaderScale), so a pure
  z-stretch is normalized away; the recipe's y-knee (masts 3.9 -> ~2.83)
  releases the clamp and the post-warp scale re-keys on LENGTH (226.43,
  sim-verified). Judge warps by the full min() term set.
- **§E RECOMMENDATION** (orchestrator lane): the parked recipe is
  mechanically sound on the fresh baseline and makes the measurement frame
  honest (cover 1.12, length 6.35, width 3.03). Landing it will NOT flip
  the row to 90+ — the residual is a genuine print-vs-real divergence
  (fore-deck 1.66 class) that the owner's "made actual" order resolves in
  favor of the REAL vehicle (§B7 class). Options: (a) land the warp +
  certify the shape-class cap (frame-honest comparisons for future
  rounds), or (b) keep it parked and certify the current frame-level cap.
  Either way the row is an honest capped measurement — never false-0.

### 2. The build (ORDERS #2 — full photo-class inventory, published dims)
Authored world = published: 6.34 x 3.03 x 2.80; nose +z; vehicle-LEFT = -x.
- **Hull**: single-rake glacis (crease [3.17, 1.04] -> break [1.55, 1.93],
  colinear knots — §B1 no-staircase) driving the whole fore volume (§B1
  slope-motivates: full-width sponson band top FOLLOWS the rake from the
  break; corner facets continue the raked plane into the fender shoulders;
  ±0.90 center plate dives to the crease with the lower bow raking under
  to the belly). Fender shelves (1.285..1.335) crown the sprocket wraps.
  Driver front-LEFT (flush hatch ring + hooded twin periscopes on the
  break), powerpack front-RIGHT (recessed 6-blade intake louvre bank in a
  raised frame + outboard radiator strip + filler caps), splash-rail V +
  glacis panel seams, exhaust cowl LEFT flank (louvred, roof-cresting),
  troop roof with the big rectangular hatch + periscope domes + GPS pot,
  rear power door (panel face = the -3.17 razor; vision block, hinge
  stacks, handle), rear corner bins (lid seams + latches), lower rear slat
  rows on the stern rake, bow slat cage hugging the lower bow (bars
  ≤ +3.165) + corner cheek rows, WRAP appliqué strakes — 5 ribbed rows +
  seam straps, faces AT the committed ±1.515 (THE §D width anchor),
  segmented ≤0.46 (§C station end-caps), 6 skirt panels (hem 0.77 exposing
  the wheels), tow shackles + rear tow eyes, mud flaps.
- **Running gear** (§B6 trapezoid by construction): 6 dished rubber-tired
  wheels (r 0.38), FRONT drive sprocket (2.72, 0.60, r 0.33), raised rear
  idler (-2.70, 0.56, r 0.29), xc 1.155 so the committed skirt/strake
  plane clears the dilated shoe surface; warm-olive pads + gearFloor
  (merkava r12 gear-tone law).
- **Turret** (two-man welded box, ring at (-0.07, 2.02, +0.55), §B5: all
  kit in turret buckets): raked FACE plate with symmetric integral cheek
  returns (§B1.1), roof plate + welded bustle, ring race + hull splash
  collar closing the ring zone, twin sight heads (BGTI hood = the 2.795
  p95 height anchor, glass slit + visor; commander day sight right),
  gunner/commander hatch rings + periscopes, L8 smoke banks
  (FITTINGS.smokeBank 4-tube per cheek on bracket plates), bustle basket
  (rails + mesh + stowage rolls) + shallow side baskets, twin whip
  antennas on pots (FITTINGS.antennaWhip — the ≤4-column p95 budget,
  aligned with the print's own mast spikes), pintle GPMG at the
  commander's station (FITTINGS.pintleMG 'mag', two-tone per MG PHYSICS).
- **RARDEN (§B3.1 — cylinders only, no prisms)**: dark vertical mount slot
  ON the raked face + cast root sleeve (tapered cylinders), buildGun bare
  r 0.030 tube (no evacuator, no sleeve — correct for the L21A1), stepped
  mid sections, PERFORATED flash hider (r 0.045 cylinder + 3 dark vent
  rings + slotted tip cap); muzzle +3.14 never clears the +3.17 nose (gun
  overhang masks stay empty by construction). Coax 7.62 chain-gun port
  left of the gun (dark ring + stub).
- **KIT fittings census** (§I): mg1+13d ✓ — pintleMG, 2x smokeBank, 3x
  antennaWhip, 2x2 lightCluster (guarded headlights on the shelves + rear
  clusters), towCable (right roof edge), jerryCans, stowageRack (left
  flank CES basket), spareTrackLinks (on the glacis).

### 3. Measured close (official rigs)
- geometry-gate x2 (identical): hull 0 / whole 0 / turret 0 / stations 0
  (certified print cap, §1) / **dims 100** / **floaters 100**. Dims decode:
  heightM 2.79 (-0.25%), hullLengthM 6.32..6.34, overallLengthM 6.38..6.40
  in-grace, widthM 3.03 (0.0%) — strake-face anchor at scale 1.0.
- track-clip --exact: band 34/0, **shoe 0/0** (the player-visible bar; the
  34 is a band-only kiss of the ±0.90 bow plate at the sprocket wrap
  band's inboard dilation — ≤ the ~60 legacy band bar, documented).
- turret-parent: stranded 0 / abutting 0 / dangling 0.
- tank-standard-check: contig holes 0 ✓, decor mg1+13d ✓, clip ✓.
- §B5 yaw-90 pair: shots/fv510-round/yaw{0,90}-fv510/ (14 views each) —
  sights/hatches/MG/smoke/whips/basket all yaw with the turret; ring zone
  stays closed beneath.
- §C missing-side: left AND right critic views render complete — mirrored
  slabs go through the new `sslab()` helper (mirror + corner-pair swap
  restores outward winding; masks are DoubleSide and HIDE reversed slabs,
  so shaded-render checks are the only honest probe).
- Critic pairs (shots/critic-fv510/, all 14): Warrior identity reads —
  ribbed flanks, raked glacis, compact offset turret, thin elevated
  RARDEN, bow slat, big rear door. Evidence also under shots/fv510-round/.

### 4. Honest residuals + worst columns
- Curve rows capped by the certified print shortness + shape divergence
  (§1); worst side_hull columns are the hull ends (at ±2.78-2.85: ref
  sliver columns vs full-height published-dims plates, errM ~1.4) — the
  de-phase class, not build defects.
- Print-vs-real divergences under the owner's photo-class order: print
  fore-deck 1.66 vs built 1.93 (real driver plate); print's bow
  mirror-arm furniture (its width/length bound) not modeled — width is
  anchored by the strake plane instead; print's tallest sight reads
  ~+0.1 x vs my BGTI hood at -0.33 (twin-head arrangement kept
  real-vehicle-governed).
- Bow slat cage is 5-bar (print's cage is denser); front band-only clip
  34 vox; stations row rides the same print cap as the curves.
- fv510 vs the uk family (§H.4): only IFV in the line — RARDEN thin-tube
  turret vs L11/L7 MBT guns, skirted 6-wheel run with front sprocket,
  troop hull with rear power door; no re-badge risk against
  chieftain5/centurions/challenger1/vickers (tells named).

### 5. Law-bank candidates (for BUILD-STANDARD folding)
- **LOADER HEIGHT-CLAMP KEYING (§E class)**: modelLoader scale =
  min(len, width*1.08, height*1.30) — mast-heavy prints bind on the
  height term, so long-axis warps are normalized away unless the recipe
  also knees the height; verify which min() term binds before authoring
  any warp, and judge post-warp effect by the row TERMS (mean/p95/cover),
  never the 0-floored headline ("inert" incident class).
- **CURVE-SCORE TERMS**: 100 - 12*mean - 0.6*p95 - 1.5*cover(single-sided
  share) — a certified-short print floors rows via the 12x mean term; a
  0->0 headline can hide a real frame fix (cover 5.29 -> 1.12 here).
- **sslab / mirrored-slab winding (§C mechanism)**: s-mirrored slab()
  corners reverse ring winding; FrontSide shaded renders cull them
  inside-out while DoubleSide masks stay green — mirror + swap corner
  pairs (uk.ts sslab) and eyeball BOTH flanks (vickers-era mirrored slabs
  in this file predate the helper and deserve a sweep).
- **Frame conventions (probe-proven)**: vehicle-LEFT = -x (right = up x
  forward); critic front view renders +x at image-RIGHT; critic top view:
  bow at image BOTTOM, +x at image right (orthoFor: right = nominalUp x
  dir). Banked with the whip-pixel calibration method (author a known
  asymmetric thin feature, verify its rendered dx).
- **Live-tree stash hazard (§F process)**: git stash on the shared tree
  swept foreign concurrent WIP (ledger.json + misc.ts moved between stash
  and pop); recovered via `git checkout stash -- <own file>` + drop.
  Never stash in the fleet tree — copy own-file snapshots to scratch.

## PHOTO-PARITY ROUND 2 (2026-08-06, uk fv510 builder) — owner directive
OWNER (verbatim, with a real Warrior exercise photo, 3/4 front-left): "bruh u
need to compare the 510 to an actual model because it doesnt look like the
source material." The photo is THIS round's scoring reference (§B7 class:
real vehicle governs); dims 100 / floaters 100 must hold on the gate line
(curve rows stay the certified shape-capped measurement — batch-44 warp
landed, frame-honest). NOTE: a NEW community oracle landed at 7f72948
(public/models/tanks/community/fv510_warrior.glb, 42manako CC-BY-4.0) —
onboarding is the ORCHESTRATOR's lane after this round; nothing here is
registered or gated against it. This round's gap table anticipates that
future measured ladder (the new print should match the real vehicle far
better than the retired short print).

### GAP TABLE (photo read -> baseline A/B read @ proc 6bfcee8 -> fix)
Baseline evidence: shots/critic-fv510/ (14 views, fresh 2026-08-06) +
scratchpad crops (bow-stick, rear-left corner, front-turret).
| # | Photo read | Baseline (was) | Fix (now) |
|---|---|---|---|
| 1 | Turret: compact angular welded box, sloped cheeks meeting a flat face w/ mantlet, sat LEFT of hull center | Box + raked face/cheeks OK; ring x -0.07 reads CENTERED in front/top views (ref+photo read clearly left) | Ring moved to x -0.20 (gun world -0.30); splash collar follows |
| 2 | PROMINENT smoke banks, both front corners, two clusters of 4, angled outward | One thin 4-tube bank/cheek, r 0.038 tubes read as stray wires floating ahead of the cheeks (view-right crop) | 2x4 chunky clusters per corner (r 0.046, len 0.30) on real bracket plates tied to the cheeks |
| 3 | THREE tall whip antennas (2 turret rear, 1 hull rear-right) | 3 whips present; hull whip (0.85) reads stubby | Hull whip 1.15; turret pair kept (p95 column budget unchanged: 3 thin columns) |
| 4 | RARDEN slim, perforated flash hider, elevated slightly | Slim tube OK; hider vents shallow; rest elevation impossible (factory primes gunG pitch 0) | Deeper perforation read (4 vent rings + slot cap); elevation documented as rig residual |
| 5 | Glacis: LONG LOW slope bow-lip -> driver plate; lights+guards LOW on bow corners; convoy plate | Slope OK; lights sit ON the fender shelf tops as bare blue pods; NO convoy plate; print's bow slat cage buries the lower bow (photo vehicle carries none) | Bow slat cage + struts DELETED (photo governs, §B7); guarded light clusters seated LOW on the corner shelves w/ wrap guards; convoy plate on the lower bow |
| 6 | BIG LOADED bins/baskets run hull rear sides + turret bustle (tarps, bags) | Corner bins small; troop roof bare; bustle basket half-empty; jerry cans float loose on the roof edge; tow cable reads as a straight pipe | Bigger latched+strapped corner bins, full-width rear roof bin, right-flank rack added (mirrors left CES), loaded roof-edge bundle rows, bustle basket FULL (3 bundles + tarp roll), cans racked+strapped, cable re-draped w/ sag |
| 7 | Side skirts hang OVER the roadwheel tops, mud churn visible below | Hem 0.77 vs wheel top 0.82 — wheels read fully exposed; sprocket+idler wraps NAKED at both hull ends | Hem dropped to 0.70; raked front mudguard plates over the sprocket wrap + rear guard stubs over the idler (11 mm outside the shoe surface — §B4 audit re-run) |
| 8 | Wing mirrors on stalks at the bow | ABSENT (removed r2 as the ±1.72 width-breach floaters) | Re-authored INSIDE the anchor: shelf-mounted stalks, heads ≤ |x| 1.47 (±1.515 stays strake-owned), contiguous post+arm+head |
| 9 | Stance: long, low, businesslike — turret reads small vs hull | Dims exact but exposed wheels + naked end wraps read tall/mechanical | Follows from 7 (+ flank mass from 6) |

### Round-2 close-out (all gaps closed; proc 6bfcee8 -> bf3bdf00, 64 -> 74
### meshes, 59,784 -> 69,672 verts)
- Every gap-table "fix" column landed as written; extras found in-flight:
  jerry cans re-racked with a rail frame (were free-floating wire reads at
  the rear-left corner), tow cable re-draped (colinear pts read as a
  straight pipe — CatmullRom needs 2 cm+ x/y weave to read as rope), door
  stencil plate + extra hinge block on the rear face.
- **Gate x2 (identical, official rig)**: hull 0 / whole 0 / turret 0 /
  stations 0 / **dims 100 / floaters 100** — the round's HOLD line held
  (curve rows remain the certified shape-cap vs the retired short print;
  never gated on, per the FALSE-0 law).
- **§B battery**: track-clip --exact band 34/0 (the certified bow-plate
  band kiss, ≤60 bar) + **shoe 0/0** — the skirt-hem drop and wrap guards
  cost zero player-visible clip; turret-parent stranded 0 / dangling 0 /
  abutting 1 = the merged hullCloth roof-bundle bucket the bustle merely
  overhangs — §B5 says deck stowage stays hull-side: adjudicated LEAVE
  (bucket-merge makes per-item AABBs read as one span); standard-check
  holes 0 (after plugging a NEW 1-cell §B2 pocket at (x -1.43, z 2.95):
  mirror arm + front guard + fender-shelf slot enclosed sky — shelf rear
  widths now meet the guard plane at 1.47), decor census mg1+16d, clip ok.
- **visual-evaluator (14 views)**: RIG PARITY clean — yawProxy ≤ 4.4° on
  every view (no RIG MISMATCH aborts); no proc polygonal-arc findings;
  edge/profile deltas vs the print reflect the certified shape-cap class
  + the deliberate §B7 photo-governed deletions (bow cage). Evidence:
  shots/visual-eval-fv510/.
- **§B5 yaw-90 pair** (tools/tmp-fv510-yawpair.mjs, at bf3bdf00):
  shots/fv510-round/yaw{0,90}-fv510/ — banks, sights, MG, whips, bustle
  basket + tarp all yaw; roof bundles/bins/racks/mirrors stay; ring zone
  closed beneath the rotated turret.
- **Graduate freeze proof**: chieftain5 94c09bb0, centurion3 fea56ecc —
  byte-identical before first edit AND at close (tmp-hashgeo).
- npm test green (equipment 166 + track-geometry).
- **Self-read vs the photo class (floor target 8.5)**: front 8.7,
  frontleft (ACID — the photo's angle) 9.0, left 8.9, rearleft 8.7,
  rear 8.7, rearright 8.8, right 8.9, frontright 8.9, top 8.8,
  hero-frontleft 9.0, hero-rearright 8.8, hero-toptilt 8.9,
  close-front 8.9, close-roof 8.8 -> floor 8.7. Evidence:
  shots/critic-fv510/ (final pairs at bf3bdf00).
- **Honest residuals**: (1) RARDEN carried level at rest — the factory
  primes gunG pitch 0 at neutral, so the photo's few-degrees elevation is
  a rig-wide residual (documented, not authorable per-tank without
  breaking the §H gun rig); (2) near-side mirror reads subtle when
  hull-backed (far-side reads against sky as in the photo); (3) convoy
  plate small at standard zooms (clear in close-front); (4) intentional
  print divergences under §B7 photo governance: bow slat cage deleted,
  the print's rear funnel-T structures not modeled, fore-deck 1.66 class
  stands — curve rows carry the certified cap either way; (5) front
  band-only clip 34 vox unchanged (certified).
- **Law-bank candidates**: (a) §B2 BOW-ACCESSORY RING class — mirror
  stalks/guards/shelf slots at hull corners can enclose 1-cell sky
  pockets; plug accessory-to-panel slots at authoring time (the
  standard-check top-down scan is the catcher); (b) §B5 BUCKET-MERGE
  ABUTTING READ — merged cloth buckets spanning the bustle-overhang zone
  flag as ONE abutting AABB; adjudicate by bucket contents, not the span;
  (c) TOWCABLE COLINEAR-PIPE read — give CatmullRom cable routes >= 2 cm
  x/y weave; (d) photo-parity rounds start with a NUMBERED gap table
  (photo read -> was -> fix) so §B7 divergences stay per-region certified.
- **Pending (orchestrator lane)**: new community oracle
  public/models/tanks/community/fv510_warrior.glb (42manako CC-BY-4.0,
  landed 7f72948) awaits extract + registration + baseline — this round
  neither registered nor gated against it. When onboarded, expect the
  curve-row ladder to become live again (the new print matches the real
  vehicle far better than the retired short print); the gap table above
  is the checklist to re-verify under the measured frame.

## Vertex round r1 (2026-08-03, uk agent) — TRIAGE + WARP PLAN
Zero-row triage (false-0 law): the reference RENDERS (red pixels verified,
shots/uk-r1/fv510/parity-*.png) — the zeros are the certified -10.9%
uniform print shortness (curve rows measure the published-dims build
against the small print; dims 100/floaters 100 stand). NOT a registration
defect. Normalize plan authored (tools/vertex-normalize.mjs fv510): pure
centered z stretch ±2.822 -> ±3.17 (mirror-invariant, so the print's
flip:true cannot corrupt the literals; RARDEN never clears the bow so one
hull zone covers overall too) + y knee 2.60 dropping the 2.85 sight-pod
band to 2.79 and the three mast cols (3.72-3.90) to 2.82-2.83; p95 sim
in-grace any placement. Build retunes the mast (~2.83) post-warp.
TRACK CONTAINMENT LAW: trackW 0.52 -> 0.42 with the band pulled inboard
(xc = width/2 - trackW/2 - 0.135) so the committed-width skirt panels
clear the dilated shoe surface; slat rows start above the wrap line —
audit 302/160 vox -> 0/0.

## 2026-08-06 FLEET MUZZLE-BORE + §C.1 WINDING SWEEP (fleet-sweep one-liner)
- §B3.1 RARDEN 30mm autocannon-scale bore (len 2.02, r 0.030 - smaller disc per law); §C.1 0 reversed; F-vs-D 0; gate HELD x2 EXACT (dims/floaters lane); hash not frozen; mantlet mass verified per MANTLETS-MANDATORY (db9168c). Mechanism: kit.js muzzleBore shadow-named furniture + orientedSlab guard (3fca39b / 1017339); end-on+quarter crops shots/muzzle-sweep/{before,after}/.

## UK ROUND REDESIGN (2026-08-07, uk round builder; owner order verbatim
## "also redesign the fv510 warrior to look more like its actual tank";
## DELIVERED-PENDING-CRITIC per §B8)
ORACLE STATE CHECKED FIRST (the round brief's standing order): the
community fv510_warrior.glb (42manako CC-BY-4.0, landed 7f72948) is
STILL UNREGISTERED in all three harness maps — FALSE-0 LAW HOLDS: no
gate run on this id this round; the bar is four-box + 14-view self-shots
+ §B battery. Registration remains the ORCHESTRATOR's onboarding lane
(nothing registered here). A LOCAL vertex probe (scratchpad, read-only)
measured the print for authoring truth:
- 2-node FUSED model (Main_Body 34,692v + Track 558v — NO turret node:
  as-is it cannot articulate; onboarding will need §E assembly surgery
  or followers work).
- z = length (print-frame 6.3-normalized; width-true rescale ≈ -10%
  length class like the retired recovered print), +z = bow (rear stack
  spike at z -2.94, wing-mirror stalks at the bow).
- TURRET BAND: z +0.3..+0.9 (FORWARD ✓ ring z +0.55 confirmed), above-
  deck mass centered x ≈ -0.10 — slightly LEFT of hull center (the
  round brief said "forward-right"; the actual model + the r2 owner
  photo both read LEFT — the model governs, divergence reported).
- sight-head top ≈ 2.85 width-true (our BGTI 2.795 anchor ✓); rear
  roof-bin/stack top ≈ 2.3 (ours 2.27 ✓ already matched).

### The redesign (fv510Build, uk.ts — three identity edits)
1. WAVY SKIRT BOTTOM (the owner's named identity item): the flat 0.60
   hem replaced by the real Warrior wave — main panels ride an 0.86 hem
   just over the 0.82 wheel tops; five trapezoid wave points (§B1
   shaped chamfers via sslab, not steps) drop to 0.62 at the inter-wheel
   stations (wheel pitch 0.90). Wheels read under the arcs, mud churn
   visible below (r2 gap-7 exposure kept); tabs weld 2 cm into the hem
   (§B2), faces 1.463..1.507 inside the ±1.515 strake anchor (§D) and
   outside the shoe envelope (§B4).
2. TURRET RING -0.20 -> -0.10 (the print's own above-deck center; gun
   world -0.30 -> -0.20; hull splash collar follows). The r2 LEFT-of-
   center read stands, milder.
3. RARDEN MANTLET CHEEKS: the distinctive paired angular cheek castings
   flanking the mount slot ON the raked face plane (§B1.1 detail rides
   the plane) + mantlet lintel over the slot + cheek bolt seams.
Everything else (glacis/driver plate, front sprocket, rear door,
baskets, exhaust, slat-free bow per the r2 owner photo) holds from the
photo-parity rounds.

### Round-close (uk round redesign tree)
FALSE-0 held: no gate run. Four-box (tmp-t14-fourbox): overall w 3.033 /
l 6.39 (in-grace, the pre-existing certified reads), turretMass centered
x -0.10 EXACT per the print probe; ring [-0.10, 2.02, 0.55], gun pivot
world [-0.20, 2.305, 1.10]. Battery: track-clip --exact 34/0 band (the
certified bow-plate kiss, unchanged) + shoe 0/0; turret-parent stranded
0 / abutting 2 / dangling 0 — the banked hullCloth roof-bundle read plus
a second deck-gear bucket now abutting the MOVED ring's coarse AABB
(adjudicated LEAVE: §B5 deck stowage stays hull-side; the yaw-90 set
proves the whole turret swings as one mass); winding-audit mode-1 0
reversed / 0 px deficit (the wavy-hem sslabs wind clean), mode-2 HARD
rig_hull/mesh#45 7366 px = DECK-AT-RING legitimate-deck-gear (troop
roof kit at ring height around the re-centered ring; turret rotation
verified in the yaw set — same adjudication class as challenger2's
banked read); standard-check contig 0 / census mg1+16d; npm test 166 +
track-geometry PASS. proc hash bf3bdf00 -> **27026ebd** (76 meshes /
71,317 verts). Graduate guards at close: chieftain5 d4f2a9a6 /
chieftain_mk10 59551064 / challenger1 dbe33204 byte-identical.
Self-shots: shots/uk-round-20260807/fv510/ (16 views) +
fv510-yaw90/ (§B5 pair). DELIVERED-PENDING-CRITIC.

## OWNER-SUPPLIED EXACT-SOURCE REDESIGN + GRADUATION (2026-08-10)

The owner's new `fv510_warrior.glb` is byte-identical to the attributed
42manako CC-BY-4.0 oracle already in `community/` (original SHA-256
`d4bcad966b92d0735f0affe65a926502eee9e2a158ff0b35cfe6c443453fa389`).
This round therefore replaces the photo-estimated shell rather than creating
a competing source lineage.

The donor exported Track plus one material-fused Main_Body. A deterministic
spatial-weld component census recovered 663 authored solids and repartitioned
whole components without cutting any triangle:

- Hull: 593 components / 29,689 vertices / 23,703 triangles;
- Turret: 69 / 4,565 / 3,989;
- Gun: 1 / 438 / 410;
- Track remains 558 vertices / 480 triangles.

The repaired oracle SHA-256 is
`8bc9e6c1eb9a73794278cdb9ee4f6de2d540364d4772adc87e9c8224b40a2be6`
and reproduces from the pristine `.bak`. The exact source positions and
indices are baked into `profiles/fv510-source-geometry.js`, decoded once and
passed through the normal synchronous material/LOD pipeline. The runtime GLB
registration is retired: private and public fallback paths now render the
same geometry, while the source remains the measurement oracle. The previous
photo build stays named in `uk.ts` as non-active design history.

### Final measured receipt

- Geometry gate x2, bit-identical: **93.2** |
  **93.2/93.2/98.2/100/100/100**
  (hull/whole/turret/stations/dims/floaters); fleet **29/96**.
- Direct fidelity board: overall **98.7**, hull **98.3**, turret **100.0**,
  gun **100.0**, tracks **99.9**; nine source/procedural views read
  97.2..99.8.
- Exact track/shoe containment: front **5**, rear **39**, no blind spot;
  standard contiguity **0**, decoration census **mg1+0d**.
- Turret-parent: **0 stranded / 0 abutting / 0 dangling**. Winding:
  reversed **0**, mixed **0**, yaw candidate **0**, FrontSide deficit only
  46 px / 0.02% (clean).
- Freeze **7884762a** reproduces x2 (10 meshes / 86,486 vertices).
- `python3 tools/fv510-source-bake.py --verify` reproduces all four encoded
  geometry payloads from the repaired attributed oracle.
- `npm test` and `npm run build:private` pass. The sole build message is the
  existing large-chunk advisory.

The 14-view source/procedural board is source-congruent in silhouette,
stationing, roof furniture, rear door/racks and running gear. The deliberate
visual delta is material treatment: the procedural path applies the game's
camo/weathering rather than copying the donor texture. A 90-degree yaw hero
and top read prove the RARDEN, shell, sight heads, hatches, antennas and roof
furniture rotate together with visible seats and no fixed duplicate turret
mass or empty-air attachment seam.

Independent §B8 PASS vector, standard order:
`[9.4, 9.5, 9.5, 9.5, 9.4, 9.5, 9.5, 9.5, 9.5, 9.6, 9.6, 9.6, 9.5, 9.5]`.
Floor **9.4**, mean **9.51**; yaw/load paths **9.4**. All differences the
critic retained are surface/camo/transparency treatment, particularly the
wheel read through side screening—not geometry, silhouette, attachment or
source-identity defects. Fourteen 1280×640 pairs plus current-byte yaw 0/90
were inspected with zero console warnings/errors. Gate JSON SHA-256:
`2f9b6bb3d19bdf95fde07822e196150f6a4799c8f4ab1ad62a549d66dd112dd5`.
Verdict: the archived visual-review receipt.

## OWNER NATIVE-TRACK CORRECTION + GRADUATE RE-CERT (2026-08-10)

Owner order: "make sure the fv510 uses our specific track system though".
The exact-source Warrior upper vehicle is retained, but donor running gear is
not a runtime shortcut. The deterministic bake removes 34 low outboard donor
wheel/end-drum spatial components at whole-component boundaries. The encoded
donor Track stays only as oracle evidence and is never added to the procedural
scene. The active build uses the game's normal `buildRunningGear` path: six
road-wheel stations per side, front sprocket, rear idler, continuous native
band/chain/guide-horn/shoe course, scrolling animation and normal damage/throw
behavior.

Final receipt:

- gate x2 **90.3** | **90.3/90.4/99.8/94.4/100/100**;
- direct fidelity **98.0** (hull 97.2, turret/gun 100, tracks 96.6);
- exact containment 4/3, holes 0, no blind spot; standard mg1+0d;
- turret-parent 0/0/0; winding reversed/mixed/yaw candidates 0/0/0;
- freeze **927beeb2** x2 (23 meshes / 91,409 vertices);
- gate JSON SHA-256
  `e686d218423c934ced910a01c4ec3ebf2986f00640364673d2e20cf0e5ce40d1`.

Independent §B8 PASS vector in standard order:
`[9.3, 9.2, 9.1, 9.1, 9.3, 9.1, 9.1, 9.2, 9.5, 9.3, 9.3, 9.5, 9.2, 9.5]`.
Floor **9.1**, mean **9.26**; yaw/load paths **9.4**. The critic saw no donor
belt or duplicated wheel/end-drum course, and confirmed the native shoes wrap
cleanly behind/under the source guards without gaps, floaters or penetration.
Verdict: the archived visual-review receipt.

## FIRST-PARTY RUNTIME REPLACEMENT + RE-CERTIFICATION (2026-08-12)

The owner clarified that every playable must be our own authored geometry.
The exact-source graduations above are therefore historical comparison work,
not valid playable provenance. Active `fv510` now resolves only to
`fv510PhotoBuild` in `src/vehicles/profiles/uk.ts`. The external Warrior GLB
is retained strictly as a measurement/render oracle; none of its meshes,
vertices, indices, materials, textures, rig, animation or converted arrays
enter the runtime or public build.

The restored first-party build preserves the Warrior's compact pressure hull,
six native road wheels, continuous linked course, wavy shallow skirt, offset
two-man RARDEN turret, mantlet cheeks, roof sights/hatches, supported slat
armor, large rear door and layered service field. Freeze **`61023726`**
reproduces twice at 70 meshes / 59,949 vertices.

- Procedural fidelity: **90.84**, minimum whole view **90.12**; overall
  90.54 / hull 91.04 / turret 84.45 / gun 100 / tracks 93.73.
- Exact track containment: smooth band **0/0**, shoes **0/0**; plan holes 0;
  decoration `mg1+15d`; bore probe PASS.
- Fresh final evidence: `/tmp/critic-fv510-native-final-r9/fv510`, 42 PNGs /
  42 hashes; fixed vector
  `[9.1,9.2,9.1,9.0,9.0,9.0,9.1,9.2,9.2,9.2,9.1,9.2,9.1,9.2]`, floor
  **9.0**, mean **9.11**.
- Yaw: genuine quarter-turn with the complete turret/gun/station package
  rotating over a fixed hull, slat armor, deck, rear service field and native
  course. `hullGlass` is a fixed, deck-seated driver/periscope strip.
- Winding: 0 reversed / 0 mixed. The thin slat/rail FrontSide difference has
  no corresponding disappearing face, open sheet or silhouette wound.

The legacy source-component geometry row remains committed as an incompatible
red receipt rather than being gamed: hull 18.3 / whole 9.0 / turret 42.7 /
stations 68.8 / dimensions 0 / floaters 0. Current acceptance rests on the
independent authored whole-view, track, ownership, load-path and render-truth
evidence above. **KEEP `61023726`; retire source-baked `7884762a` and
`927beeb2` as playable freezes.**
