# T-90M family (`t90m`, `t90m_proryv`) — reference packet

Current roster mapping (2026-08-27): `t90m` is the retained tier IX
**T-90M**, while `t90m_proryv` is the new tier X **T-90M Proryv** with the
two-row chevron Relikt front. Both use the corrected longer T-90 chassis;
the historical source below is a comparison oracle, not playable geometry.

Variant: T-90M Proryv (obr. 2017) — welded turret replacing the T-90A cast
shell, Kord 12.7mm RWS on the roof, bustle stowage boxes, Relikt ERA on
skirts and turret cheeks, 2A46M-5 with thermal sleeve.

## Provenance
Local oracle `/models/tanks/t90m_minehffd.glb` — "T-90M" by minehffd,
CC-BY 4.0 (license stamped in asset.copyright + scene extras; attribution
recorded in `docs/ATTRIBUTION.md`). Authoring tools may load it with turret
`^Turret$`, gun `^Main_barrel$`, automatic pivots, overall-length scaling,
and a +90° yaw. The playable registration is always procedural. Known oracle
minor: the mantlet is a turret sibling mesh and stays put while its barrel
elevates.

## Real-vehicle dimensions
- Hull 6.86 m, overall 9.63 m (gun forward), width 3.78 m, height 2.23 m
  (turret roof line, Kord excluded) — T-90/T-90S family figures
  (rosoboronexport datasheet class; same numbers registered for t90sm in
  userdrops5.js).

## Local GLB oracle — extract state (REG batch 0a39d55)
Pre-warp: width TRUE 3.78 (0%); roof line nearly true (turret roof cols
2.25-2.32 vs pub 2.23); bodyH read +25.9% (2.807) — ENTIRELY the wide
furniture band (Kord RWS + bustle bins) crest 2.72-2.97 over gate-z
-2.4..-1.2; hullMask 7.482 (+9.1%); overall 9.891 (+2.7%, tube slightly
long relative to the compressed-body target).

Warp (repair batch-23, vertex-normalize PLANS t90m): knee 2.20 — roof
untouched, furniture band compressed to 2.20-2.253 (post-map p95 ≈ 2.24,
inside the 1% dims grace; y_top_max assert 2.26). Body z ×0.9169 about body
center -1.2045 (hullMask -> 6.86); muzzle pinned at rear+9.63 (tube segment
stretches ×~1.15 to absorb the body compression — abramsx-class pin).

## r26 FIRST BUILD (2026-08-03): zero row -> 64.7 min / whole 73.1

buildT90MProryv in src/vehicles/profiles/russia.ts (registered in
RUSSIA_PROFILES — overrides the legacy tankFactory buildT90M via the
PROFILED_BUILDERS merge). Final row (gate x2 stable): hull 65.0 /
whole 73.1 / turret 64.7 / stations 69.0 / dims 98.2 / floaters 100
(min 64.7). standard-check: holes 0 ✓, mg census 1 (Kord as
FITTINGS.pintleMG on a recessed pedestal — crest 2.251 inside the roof
plateau band, §I); clip 581/244 vs the ≤60 band — same strip/sponson
class the certified graduates read (pt91m 178/220, t72b3m 863/301
today); needs the fleet ruling.

ORACLE GATE-IN-LOOP CHECK (the round's mandate): the re-warped oracle is
HEALTHY — rendered ref masks contiguous in all rows, stations compare
within a few %, the warped furniture band holds 2.20-2.25 as documented.
The pre-round all-0 ledger row was the LEGACY canonical build (height
2.52 vs pub 2.23), not an oracle defect. One ref-side surprise, banked
below: the EXTRACT's own curve tables disagree with the rendered ref in
two places (bow plan corners; skirt-course span) — §D render-outranks-
rows extends to the extract JSON.

World frame = extract + 1.38. What the build carries (decode-corrected):
- Bow: plan center 3.05 / corner prongs 3.27 (extract corners said
  3.20/3.44 — RENDER WINS), mud flaps at the 0.66 bow floor, slim center
  bow probe (tow-hook cluster) 3.34..3.40 = the hullLengthM body anchor
  hidden behind the gun's plan shadow (its 2 naked plan_hull columns are
  the accepted price; dims 98.2).
- Rear: full-width plate -2.90, RACK to -3.43 at |x|<=0.99 (floor 1.17,
  transverse drums, spare bin, stays to 1.80 at ±1.0 — ref front cols),
  CENTER NOTCH (ref center-rear is -3.11: split log/mesh/bin + tow
  hitch), unditching log slung at -3.44 (rear -3.53; ref plan rear -3.59
  at |x|<0.9; overall 9.74 = +1.13%).
- Gear: wheels at the ref's ground contacts (evenStations 4.01/0.515 =
  -1.49..2.52), sprocket -2.10, idler raised (0.78/0.22 — its wrap
  bottom owned the 3.05-3.25 columns), track xc 1.435 / trackW 0.58
  (LINK OVERHANG: shoes reach ±1.748 vs the ref's 1.73 band), arms off,
  strips on the rendered ramp lines (rear jumps 0.52@-2.68 -> 0.86 plate
  line).
- Skirts: thick band (0.09) at faces 1.77..1.86 y 0.78..1.36 + the
  Relikt COURSE as thin outboard bags at ±1.89 (y 0.48..1.34, z
  2.46..-2.66) — the rendered ref's widest, near-full-length content
  (the extract's plan row said rear-only: WRONG, render wins). Bow
  mirrors deleted: the ref's 4 cm mirror flickers between grid phases
  (present in one run's columns, absent the next) — carrying it costs
  0.35 err when absent.
- WELDED turret: polyTurret staircase planform (chamfered corners,
  inset 0.93 — near-vertical walls; 0.80 read 0.3 narrow at the roof),
  roof TIERS from the rendered profile: 2.245 plateau z -1.11..-0.03
  over a full-width 2.20 tier (±1.15), 2.20 step to +0.51, 2.03 to
  +0.93, 1.94 wedge to +1.48, mantlet hood <=1.71 (plan front law);
  hidden turret-node APRON (print bakes hull kit into the turret node)
  bottoming 0.88 across fitted [-0.92, 0.87] with half-height end
  treads (CLIFF-LERP law); bustle bins as SEPARATED members: upper tier
  1.51..2.01 to -1.9 wide ±0.95, tail tier to -2.43 RIGHT-DEEP (ref
  rear staircase is asymmetric, left ends -2.02), rails + door -2.45.
- 2A46M-5: axis 1.61, sleeve 0.118, evac r 0.128 + crest fin (the 0.138
  cylinder's raster leaked the ±0.19 plan columns at its full z-run —
  PLAN RASTER LEAK law), muzzle +6.20 exact.

Honest residuals for the next round (whole 73.1 / turret 64.7):
- turret plan +0.19 col err 0.84: proc front-v reads z 3.44 at ±0.19 —
  no authored turret mesh exists there (whatsat-clean); suspect the
  trace/interp seam or a proxy; needs a mask dump before more geometry
  is spent on it.
- turret side 1.06/0.93 cols (~0.3): apron front-edge phase vs the
  settled dAlong 1.363 — one more boundary nudge after the next
  registration-stable run.
- hull 65: front rows carry the remaining glacis-top skew (ref 2.02-2.17
  at |x| 1.1-1.4 vs prism walls) and the belt flat-end ground reads at
  z -1.9..-2.4 / +2.9 (fleet-certified partial class).
- stations 69: i10/i12 tops ~10% — ref-side slice content (its z-range
  is its own hull mask) reads the fused-mantlet mass in slices my build
  keeps gun-parented; structural to the node split, price ~1.4.

## Build notes (for the russia-family builder)
- Profile-ize vs t90sm/t90a: the triage probe shows the canonical T-90M
  build already boards ~75 as a donor; the welded-turret planform (flat
  cheeks, chamfered front corners) is the main identity delta from the
  t90a cast dome.
- SEPARATED FURNITURE: build the Kord RWS + bustle boxes as thin members
  (post-warp ref keeps them at 2.20-2.25) — decoration law satisfied by
  the Kord (mandatory roof MG) + bins/ERA blocks.
- Relikt skirt line and cheek wedges read in the side/plan masks — follow
  extract corners, not the t90a profile's K-5 bricks.

## r27 WORKORDER ROUND (2026-08-04, russia agent — r29 respawn session):
## 64.7 → 67.9 min; both r26 ordered residuals RESOLVED

Gate at close: **min 67.9 (hull 70.6 / whole 75.4 / turret 67.9 / stations
69.6 / dims 98.2 / floaters 100)**. Trajectory by component across the
round's six gate cycles: turret plan 82.2→86.5, front_hull 72.2→78.9,
hull 65.0→70.6, whole 73.1→75.4, turret side 64.7→67.9, stations 69→69.6.
standard-check: holes 0 ✓ (two 1-cell regressions from the trackW trim
were plugged same-round), mg 1 ✓, clip 296/249 (improved from 581/244;
still the strip/sponson fleet-ruling class).

THE r26 MASK-DUMP ORDER (turret-plan ±0.19 col, err 0.84) — DECODED AND
FIXED, two stacked mechanisms:
1. The evac swell seg (r 0.128) raster-clipped the ±0.17/0.201 plan
   columns over its world 3.20-3.44 z-run — the same mechanism the r26
   note documents for its earlier 0.138 cylinder (col inner boundary
   ±0.108; 0.128 still clips 20 mm). Swell → the tube's proven-quiet
   0.100; the crest FIN alone carries the ref's 1.75 side crest.
   MEASURED: the 0.843 entry died; turret plan 82.2 → 85.9 on the next
   gate run.
2. The GUN SHADOW PROXY ran to world 7.28 — spec.armor.gunBarrel.lengthM
   6.0 predates this profile (authored muzzle 6.20 = ref-exact). The r26
   "no authored mesh exists there" mystery: proxies disable raycast, so
   whatsat can't see them. Spec datum aligned in-builder (4.92);
   probe-verified z 1.28..6.20. **FLAGGED FOR ORCHESTRATOR RATIFICATION**
   (consumers audited: proxy + muzzleZ default (overridden) + UI framing).
   LAW-BANK CORRECTION: an A/B mask dump (proxies visible vs hidden)
   measured ZERO poisoned columns in side/front hull masks — §C's
   "shadow-proxy meshes ARE in gate masks" is STALE for the current
   harness (renderMask filters them like the shared-box isShadowHelper
   filter). Proxies still matter to the vertex-workorder's raw traces.

THE r26 APRON-PHASE ORDER (side 1.06/0.93 cols): the front tread's bottom
sat 0.34 low (1.04 vs the ref's 1.38 line) — slimmed to a 6 cm step ON
the 1.38 line; the carrier rear extended to the r26 packet's OWN fit
(−0.95; it was authored to −0.66 while the packet fitted [−0.92, 0.87]).

FURTHER DELIVERED:
- idler 2.90 → 2.65 (+5.6 hull in one cut): the ref's side-hull bottoms
  at z 2.99-3.36 are its RAMP LINE — the r26 fade strips match it exactly
  (0.50@3.06 / 0.57@3.14) — and the five worst side_hull columns were my
  idler wrap + belt dive hanging below that line, 0.4 m forward of the
  ref's own gear end (~2.6; contact stops 2.52).
- trackW 0.58 → 0.50: the link shoes reached ±1.748 and painted the
  ±1.76/1.77 front columns to the ground (err 0.351 ×2) where the ref
  bottoms at its 0.72 skirt lip. Both entries left the worst list.
- rack stays narrowed to the ±0.94 col (x 0.90..0.99): the 0.16-wide
  boxes also painted the ±1.04 col whose ref top is 1.371 (err 0.202 ×2)
  while the ±0.94 col's ref (1.75) keeps wanting them.
- turret plan front: planform nose 1.75 → 1.50 local + mantlet hood slim
  0.80 → 0.44 + cheek course sz 0.95 → 0.80 — the CHEEKS owned both the
  ±0.32-0.57 plan-front columns (ref 1.64-1.79) and the z 1.5-1.78 side
  tops (ref 1.81); the planform taper alone measured nothing because the
  cheek course prints there. Hood front (1.84) carries the ±0.17 cols
  (ref 1.883).
- belt front terminus: EXTERNAL contact point (zF 2.98) past the idler
  wrap kills the near-vertical dive chord at the old cF=idler.z default;
  rear point inside the sprocket wrap degenerates to the 190° fallback =
  byte-identical rear loop, matching the ref's grounded −1.9..−2.4 warp.
- sponson-floor strips (±1.74, inside the skirt band's y-band): plan-only
  fill for the two 1-cell holes the trackW trim opened.

NEGATIVE RESULTS (documented so the next agent doesn't repeat them):
- apron aft-extension to −1.47 world: −14.6 turret side in one run (five
  columns of 0.79-bottom where the registered ref bottoms 1.38-1.44).
  The at-frame that motivated it is RUN-RELATIVE (shared box moves with
  geometry) — never carry at→world offsets across an edit.
- belt contact at the wheel centers ({2.60, −1.60}): −2.3 side_hull. The
  warped ref's belt IS ground-flat at −1.9..−2.4/+2.9 (the r26 note reads
  the REF): flat botY matches it; only the FRONT terminus chord needed
  taming.
- rear-rack lowering (drums/bins to 1.42/1.55): −0.35 ×2 at z −2.83..−2.96
  — wrong-frame decode; r26's 1.89 tops are ref-matched. Reverted.

Honest residuals toward ≥80 (turret side 67.9 is the min):
- turret side: the roof-tier band (procTop ~2.0 vs ref 1.81-1.99 over
  z 0.3-1.0 — needs the prism/tier split re-derived against a fresh
  vertex-workorder, ~4-6 cols); the bustle ONLY-x trio (cover 0.97).
- stations 69.6: i10/i12 tops ~10% ×2 = the fused-mantlet node-split
  class (r26 priced ~1.4, structural); i0/i1 rear reads unresolved.
- side_hull 70.6: remaining mass is diffuse (meanPct ~2.3 vs a warped
  ref; no single column >0.16 after the idler fix).
- dims 98.2 unchanged (bow-probe plan cost, accepted r26).

## r30 WORKORDER ROUND (2026-08-04, russia agent): 67.9 → 81.7 min ×2
## (hull 81.9 / whole 81.7 / turret 83.3 / stations 81.9 / dims 100 / fl 100)

standard-check: holes 0 ✓, mg1+2d ✓ (Kord + 902B banks as FITTINGS), clip
222/188 (improved from 296/249; still the strip/sponson fleet-ruling class).
Shots: shots/visual-eval-t90m/ (14 views + report.json).

FRAME LAW (this round's engine — supersedes every per-run at→world recipe):
the gate JSON's `at` decodes as authored_z = C.z + dAlong − at (side; C =
TRUE shared-box center, dAlong = the row's own reg), y = v + C.y. The
vertex-workorder's printed side coordinates carry a +0.63 bias THIS class of
run: its `vb()` recomputes the shared box while the page's LAST renderMask
leaves reference.root invisible → C degrades to the PROC-ONLY box center.
Its side rows also pair columns RAW (no dAlong shift) — with this print's
scene offset (~1.36) every printed side gerr is frame-smeared. Bespoke
digest (scratchpad gate-digest.mjs pattern) replicates curveScore's exact
registration and prints both models in the authored frame; official gate
stays the verdict rig (§D).

Delivered (registered-row driven, per-edit gate deltas in git history):
- ROOF TIER STACK: prism hm 0.59→0.44 (walls 1.84); tiers 2.2375 humps
  (split ±0.51 w/ 2.07 center saddle — ref front center cols read 2.07,
  two-hump roof) / 2.20 step / 2.03 to 1.10 / 1.90 to 1.35 / 1.81 to 1.66
  / hood 1.51..1.71; asymmetric shoulders (L 2.01, R 2.20 caps at ±1.08;
  ref roof is left-low) + shoulder caps 2.07@±1.23, 1.98@±1.38 (z-short,
  inside the ref plan-turret rear line). Cheeks single-row (rRows 1 — the
  upper cassette course crested 2.0-2.05 where the ref roofline steps;
  the ref's cheeks stay UNDER its roof tiers).
- STATION END-CAP LAW BITES 3×: the 2.76 m tube seg had no z-caps inside
  stations i11/i12 (front-clip renders z-cylinders edge-on; only END CAPS
  paint) → tube now 10 segs ≤0.55 m; bustle upper tier split at −1.74
  (its cap left i3's window → topPct 25.5); plateau humps split at −0.80
  (i5 had no cap → 4.94). Stations 69.6 → 81.9.
- BUSTLE: upper tier to −2.15 (ref holds 2.00/1.51 through the −2.04..
  −2.16 col), tail tier 1.62..1.87 to −2.38, low rack bar to −2.445 top
  1.40 (ref's z −2.47 col is deck-line 1.38, NO turret band, while its
  plan turret runs to −2.42 — the old 1.94-to-−2.43 + door −2.46 printed
  1.90 there, the top side_whole item 0.27).
- CARRIER: rear −0.95 → −0.88 (at-3.04 col held a 0.79 bottom vs ref
  1.37); bottom 0.80 → 0.88 (the ref apron line — r26's own packet fit;
  five 0.066 cols).
- TRACK CONTACT LAW FIND: `contact:{zF,zR}` (r27) is a SILENT NO-OP —
  tankFactory reads cfg.contactZF/contactZR only (defaults wheel-span
  ±wheelR/2 = 2.71/−1.68 here). Both r27 contact "results" (the credited
  dive fix AND the −2.3 revert) measured other same-batch edits. Real
  values: contactZF 2.45 / contactZR −1.50 (ref belt lifts 0.16@2.74,
  0.15@−1.85, 0.22@−1.97, 0.29@−2.09; pad tips sag ~0.07 below the band
  tangent, so the tangent start sits ~0.1 behind the ref's visual lift).
- SKIRTS: band re-span x-faces 1.762..1.842 / z −2.61..2.55 with a front
  TAPER (1.28@2.68 / 1.17@2.91 / 1.12@3.14 — the flat 1.36 top ran to
  3.28 where the ref side line falls; five 0.10-0.15 cols) + Relikt
  course half-bag to 2.80 (ref's widest content reaches 2.81@±1.89;
  the r26 "rear-only" extract row stays wrong, render wins); bag hems
  back at 0.48 (the r30a 0.60 lift chased a stale dy).
- REAR RACK staircase to today's registered reads (drums 1.76 / stays
  1.72@x .8975..0.9875 / bins 1.84 z −3.04..−3.34): NOT the r27
  wrong-frame lowering — a 5-8 cm trim; center drum-coupling box (top
  1.61 — ref front-view center cols; the drum pair's x 0.105 inner ends
  also painted the ±0.11 front col to 1.76 → drums start x 0.15). Floor
  plates x 0.145.. (0.12 sat ON the ±0.12 plan col boundary; its AA
  bleed painted the center cols to −3.41 → plan_hull 0.31 pair). Log at
  −3.41 (overall 9.70 = +0.77%, dims cost 0); rear flaps at ±1.55
  y 1.08..1.32 (ref plan rear −3.00 at |x| 1.36..1.73).
- FRONT: flaps widened x 1.4525..1.8425 @ z 3.2425..3.2875 hem 0.71
  (ref plan front 3.28@±1.8 / 3.33@±1.5 with side tops ≤1.09); fender
  lips i=1..8 only (fwd pair deleted — ref glacis-top line 1.29@2.25 →
  1.23@2.62; wedge rows −6 cm); periY 1.24 + hatchY 1.27 + hlY 1.19
  (near-flush class, t72b3m lesson); glacis kit re-seated.
- DECOR/§B3: Kord crest 2.243 (the r30c plateau restore had left the
  crest 2.211 BURIED under the 2.2475 plateau — un-buried, then heightM
  p95-guarded: crest cols ~4 > the 3.25-col p95 exclusion at 2.27);
  902B smoke banks on the cheek flanks (tips ≤ x ±1.786, tops 1.73 — the
  smokeBank xform pivots tubes at their CENTERS: tips gain only
  len/2·sin(a); first seat undershot by 0.09) + cheek stowage baskets
  (ref front dome 1.84-1.90@±1.64, z-end 1.02 = ref plan line).
- 2A46M-5: root cone 0.50 (its 1.76 crest cap sat over the ref's 1.70
  tube line at i11); evac fin 0.25 band (0.276 qualified as a body col
  and dragged hullLengthM to 6.94).

NEGATIVE RESULTS (measured, reverted):
- 2.30 tier narrowed to 1.96: front_whole 77.9 → 60.2 in one run (ref
  front carries 2.16-2.21 across ±1.0..1.15 — the ±1.04 "ref 2.00" read
  was the LEFT side only; the roof is asymmetric).
- plateau 2.245 → 2.21: chased a 256px-quantized digest read; the 1024
  gate says the ref turret line is 2.24 (restored, then heightM-guarded
  at 2.2375).
- Kord crest 2.27: became the heightM p95 (pct 1.62, dims −5).

FLAGGED FOR ORCHESTRATOR:
1. `contact:{zF,zR}` no-op (above) — either alias it in tankFactory or
   sweep profiles for dead configs; r27's contact conclusions need
   re-attribution.
2. visual-evaluator TOP-view RIG MISMATCH on t90m is a FALSE POSITIVE:
   near-±90° plan principal angles land on opposite atan2 branches
   (ref +89.978° / proc −89.998°) and the skew-flip test compares raw
   skew signs without normalizing eigenvector direction — normalized,
   refSkew −0.168 ≡ procSkew −0.176 (same physical rear-heavy skew);
   dPrincipal 0.024°, dCentroid 0.03 m, dArea 0.3%, other 13 views
   yawProxy ≤0.9°. Evaluator needs a direction-normalized skew (e.g.
   flip e when principalDeg < 0) before the top-view abort is trusted
   on plan-symmetric prints.
3. workorder tool: restore both roots' visibility before its vb()
   (the C bias above) and apply dAlong when pairing side columns.

Honest residuals toward 90 (all rows, worst first):
- side_hull 81.9-min class: belt-rise tails (±0.05-0.08 over z 2.6-3.0
  after the contact fix), painted sprocket end-board fan edge ~0.6@−2.7
  (ref's baked fan is smaller), diffuse 0.05-0.1 mid-deck tops.
- front_whole 81.7: right-bank inboard dome gap (ref 1.90@1.64, one col
  0.10 — a 1-col riser violates the 15 mm law, left unfixed), band-face
  col ±1.81 top 1.36 vs ref 1.26 (ref panel-seam dip, 1-col ×2).
- turret side 83.3: plateau/step micro-steps ≤0.05; plan_turret 88.7:
  hood front vs the plan dy drift (re-measure after any hull plan edit).
- stations 81.9: wPct i2/i4 3.38 (dropped by trim; band-rear/bag zone),
  i9/i11 2.24 (bank tips / bag width vs ref 3.69).

## §B4 CONTAINMENT round (2026-08-05, russia agent): clip 222/188 -> **0/0**
## (target was the kv2 band <=60), gate 81.7 HELD x2 to the decimal —
## hash 3553395c -> 5afa239c

Round-start baseline on the official instrument: front 222 / rear 188
(the work-order's 296/249 predates r30's own improvement). Decomposition
(tools/tmp-ru-b4-census.{html,mjs}, per-add census; band decode zMax
2.960 / zMin -2.469 / yTop 1.15 / laneInnerX 1.185; idler c(2.65, 0.78)
outer r 0.31 -> crown 1.09, arc-above-sponson z 2.342..2.958; sprocket
c(-2.10, 0.78) outer r 0.37 -> crown 1.15, arc z -2.469..-1.731):

1. REAL interpenetration — loftHull UPPER SLABS (russia.ts L103 pieces;
   official rig_hull 145 front / 84 rear): the flat 0.81 track-bay roof
   buried both wrap crowns and the full-width slab side walls (±1.60,
   inside the 1.185..1.685 band x-window) carried the arcs through the
   lane (census: front 94+58+13 vox at y 0.82..1.08; rear 41+40+20 at
   y 0.82..1.14).
2. FALSE-FLAG — gear-fade strips (L3258 loop, 18 z-stations/side at
   x ±1.145..1.725, official 44 front / 104 rear): in-lane gear trim
   deliberately bedded in the band (the t72b3m "strips must stay
   bedded" class — the ref fade line is r30's own authored ramp read),
   merged center-spanning in hullDark (reach 0).
3. FALSE-FLAG — ruGlacisKit tow-eye tori (L306, 16/17 vox front, x
   ±1.159..1.361): same class as pt91m, 'detail' material.

Fixes (t72b3m recipe transfer, zero gear params moved):
- SPONSON WINDOWS (real fix): loftHull sponsonY [[z,y]] profile —
  0.81 kept fleet-wide except crown+0.03 windows over the wraps ONLY:
  [[-2.54,0.81],[-2.50,1.18],[-1.72,1.18],[-1.66,0.81]] rear,
  [[2.26,0.81],[2.32,1.12],[2.99,1.12],[3.02,0.81]] front. Knee knots
  seated OUTSIDE the arc z-ranges (knot-cut-face law; rear knees clear
  the -2.469 band edge by 31 mm, front by 22 mm). Interior everywhere:
  side rows blind to the roof (deck 1.35-1.39 above), front-view fills
  max-over-z (bow face keeps its 0.81 floor from z 3.02; band/prong/
  flap content owns the window cols), tub top rises to the window roof
  at wLo <=1.08 — inboard of the 1.185 band window. The census's
  L103 class is GONE after the change (not reclassified — removed).
- STRIPS -> hullTrackTrimL/R (the e3918e6 buckets, first non-t72b3m
  caller): byte-identical transforms, same 'dark' material instance +
  LOD path; honest one-sided AABBs (reach 1.145 > 1.035) read as the
  in-lane gear trim they are.
- TOW EYES -> hullTrackDetailL/R via ruGlacisKit eyeSplit (opt-in; new
  BUCKET_DEF pair, 'detail' slot — see the pt91m §B4 section, one
  landing).

Verify (final bytes):
- track-clip-audit --exact 0/0 (was 222/188; standard-check agrees).
- gate x2: min 81.7 both runs, components 81.9/81.7/83.3/81.9/100/100 —
  the r30 row TO THE DECIMAL twice; floaters 100 x2. The sponson
  windows are mask-invisible as designed.
- standard-check: clip 0/0 PASS | contig 0 (top-down holes stayed 0 —
  §B2 clean through the roof lift) | mg1+2d (Kord + 902B banks).
- 14-view pixel-diff vs before (same critic rig, determinism proven on
  pt91m same sitting): structured >8-level deltas ONLY in the §B4 gear
  zones (view-front 438 px = the 2-px window-riser line behind the bow;
  close-roof 337 / hero-toptilt 253 bow-window roof faces; rearleft/
  rearright ~60 sprocket-quarter each; view-rear 0), everything else
  the documented sub-visible ±1-2-level re-cut jitter class (loft knot
  insertion re-pitches the 0.36 m subdivision -> bakeDirt vertex
  interpolation shifts; t72b3m precedent). Side profiles: 4/5 px >8
  total. §B6 trapezoid intact (gear cfg untouched, side reads hold).
- hashes: t90m 3553395c (46/71629) -> **5afa239c (50/72133)** (+4
  meshes = trimL/R + detailL/R; +504 verts = the window loft re-cut);
  t72b3m 3d92bb98 / t84 fd0bca6c / pt91m-side proofs in the pt91m
  section (same runs).
- evaluator: pt91m spot ran clean same sitting; the t90m run hit a
  TRANSIENT vite 500 on modern3.ts (another agent's file mid-edit —
  not this round's graph) — retried after; the known r30 top-view
  RIG-MISMATCH false positive stands as flagged for the orchestrator.

Evidence: shots/russia-b4-t90m/{before,after}/ (14 views x2),
shots/russia-b4-census-baseline.json + -after.json, shots/track-clip.json.

LAW BANK (round discoveries):
- The eyeSplit class extends the per-side-split law to NON-dark
  materials: any in-lane per-side fitting merged into ANY center-
  spanning bucket false-flags; the fix is a per-side bucket pair in the
  SAME material slot (BUCKET_DEF one-liner), never a material change.
- A sponsonY window's knee faces can legally cross the EMBEDDED top-run
  ribbon mid-hull (outside the audit zones, interior visually) — the
  fleet-standard config already runs the return band inside the upper
  slab; only the WRAP-ZONE arcs are §B4 surface.
- Stale work-order baselines: re-measure before decomposing (both
  tanks' briefed numbers predated intervening rounds; pt91m 178/220
  was actually 24/0, t90m 296/249 was 222/188).

## §B3.1 PRISM SWEEP round (2026-08-06, russia-family builder)
PRISM INVENTORY (found -> replaced-with):
- root block box(0.60,0.30,0.30) -> cast collar (elliptical frustum,
  same plan ±0.30 / side ±0.15 extremes at center axes), fold ring
  inside the local skin, clamp at the cone->tube seam (top 1.734 world
  under the certified 1.74 hood line).
Already-real inventory (no action): 902B smoke banks = FITTINGS tube
clusters, Kord RWS fitting, Sosna-U housing with glass, Relikt cheeks
(weldFlat), mantlet hood boxes carry certified rows.
GATE HOLD x2: 81.7 | 83.1/81.7/83.3/81.9/100/100 (headline exact;
ledger hull row 81.9 was stale — pristine HEAD also reads 83.1).
npm test green. mg1+2d census held.

## §B3.2 DENSITY + T05BV-1 RWS round (2026-08-06, russia-family builder)
The T-90M roof gun is the T05BV-1 REMOTE weapon station — RWS grammar
built around the existing census Kord fitting (§B3.1 cylinders law):
slewing ring on the pedestal, sensor-head DRUM + rim + lens (x 0.21..0.31,
z -0.79..-0.65 — clear of the receiver band), sensor yoke, cradle cheek
plates, RWS ammo bin + lid seam. Every part interior to the L/R roof-hump
cover (0.8375 over x 0.20..0.82, z -1.24..-0.16; all tops <=0.77).
ADDED KIT: PKT coax port stub + washer inside the root collar's plan
rectangle (±0.30 to z 0.31); spare track-link run FLUSH at the 1.384
deck; tow cable draped flush on the left deck (tops <=1.370 vs local
lines 1.361-1.381).
MG census: mg1+2d -> mg1+4d (902B smoke banks kept). GATE x2:
81.7 | 83.1/81.7/83.3/81.9/100/100 (baseline EXACT).
Turret-parent: the "(unnamed)(49%)" stranded flag is the merged hull
bucket — PROVEN PRE-EXISTING at HEAD via stash-audit this round (§B5
whole-bucket merged-loft false-flag class); my fittings carry names and
were not flagged. npm test green.

## PERFECTION round (2026-08-06, dedicated t90m agent): 81.7 -> **90.7 PASS x2**
## (hull 90.7 / whole 91.3 / turret 93.2 / stations 94.4 / dims 100 / fl 100)

GATE x2 IDENTICAL to the decimal at the close bytes; standard-check clip
0/0 + contig 0 + mg1+4d; track-clip --exact 0/0 band AND shoe, blind spots
0; npm test green x2. Hash: 9db3e891 -> **4acdebe4** (49 meshes, 76609
verts). Frozen sibs byte-identical at open AND close: pt91m 2cf10e23,
t72b3m 1e1ca4b8, t84 04707a9c. Evaluator: all 14 views, yawProxy <=1.1
deg, no RIG MISMATCH (the r30 top-view false positive did not fire).
Official winding-audit (SSC.1): rev 0 / mix 0 / FrontSide deficit 0px;
yaw-stranded audit HARD candidates adjudicated = the rear fuel-drum rack
meshes (hull rear plate furniture the bustle overhangs — SSB5
hull-correct, documented negative).

LADDER (per-batch gate deltas, decode-first per the mission brief; the
scratchpad gate-digest at top=96 + a station/whatsat probe with fitting
tags were the work-order instruments; official gate stayed the verdict):
- r1 (81.7 -> 83.7): padCornerFloor 0.012 + padHugZ0 2.0 (centurion r6
  class: pad sag -0.015 biased EVERY station topPct +0.67% and read ~28
  front cols 0.04-0.05 low); deck flattened to the ref's 1.365-1.367
  line; lips 1.36/outer 1.765; band face OUT to the ref's 1.866 station
  line + yTop 1.30 + dressIn 0.09; course hems 0.63 + STATION END-CAP
  segmentation + course gaps at the ref's band-only slices; LEFT band
  tail to plan -2.96 (print asym); low flap hangs (ref side bottoms
  0.406/0.499 behind the sprocket); rack z-map re-seat (drums/bins/
  straps/log/floor); rear flaps widened to |x| 1.10..1.70; prong split at
  the 3.155 col boundary; rear grille slimmed to the 1.088 line.
- r2 (-> 85.5): bustle rebuilt on the registered staircase (mid 1.92 /
  low 1.708 tiers); rail strip deleted (its 1.31..1.40 band was an
  ONLY-PROC cover col + a 0.256 bottom err); smoke banks -0.26 z (ref
  plan tips 0.659..0.814); cheek course sz 0.72; shoulder boxes/caps
  asym (L 2.04/2.09/2.10 staircase, R 2.20 + 2.00 cap); 2.03-step ends
  1.03 + new 1.95 tier; 1.81-tier x-split; root cone 0.145/0.40; evac
  fin 1.522..1.708 exact; tube r 0.102 (PLAN RASTER LEAK respected).
- MANTLET LAW fold-in: the elliptical collar is now the ruBoot accordion
  (4 tapered canvas sections + crease collars + clamp) at the collar's
  certified extremes; deepened r5 to the ref's 1.46-1.48 root underline.
- SSB3.1 MUZZLE BORE: counterbore rim torus (outer 0.082) + near-black
  disc r 0.062 = 0.61x tube, faces +0.5 mm past the 4.92 cap (leopard r9
  sub-half-pixel class); end-on bore read confirmed in close-front.
- r3b (-> 87.5): straps RESTORED to the -3.425 face after the r3 trim
  collapsed hullLengthM to 6.69 (-2.42%, dims -11.4) AND re-registered
  dAlong 1.364 -> 1.424 (see LAW below); strap tops to the ref 1.644.
- r4 (-> 88.4): bustle-edge CO-LOCATION at the ref's -2.30 teeter
  boundary; 2.02-tier center split (ref L 2.27 sight head / R 2.10
  course); L-mount split 1.94/1.88; contact knee sweeps; prism nose
  1.28 + rear-flank asym pts (R -0.575/-0.12, L -0.46/-0.05); first
  bustle tier bottom 1.44; hump rear step 2.18; bins/drums re-span.
- r5 (-> 89.6): hem SEGMENTED 13 chunks (STATION END-CAP — the 5.16 m
  hem vanished from mid slices, stations i9/i11 5.45); twin sight heads
  (L -0.09 / R 0.16, caps 2.257 — inside the 3-col heightM p95 budget);
  arms split-tops; cassettes rY 0.13 (bottoms to the ref 1.40 line).
- r6/r7 (-> 90.7 PASS): half-bag top 1.26 (its 1.34 crest owned the
  2.60/2.72 cols); muzzle REFERENCE COLLAR as an ELLIPTICAL drum (y-half
  0.124 side read, x-half 0.104 inside the +-0.108 plan boundary); SSC
  half-pixel margin sweeps against the gate's own grid (step rear
  -2.395, hang2 rear -2.765, loft rear -2.908, collar end 6.12).
- SSB4 close: flap hangs moved INBOARD of the shoe lane (x 1.06..1.18);
  sponson window roofs +0.03 (padCornerFloor raised the wrap-pad crowns
  into the old crown+0.03 roofs — the blind-spot class, now 0/0).

NEGATIVE RESULTS (measured, reverted):
- Bow probe extension to the ref's 3.43 nose: lit the 3.464 body column
  -> hullLengthM 6.94 (+1.22%) AND dAlong 1.363 -> 1.427 re-phased every
  bustle target (side_whole 86.6 -> 78.7). The digest-frame ONLY-REF col
  at ~3.46 stays an honest residual; the official gate never priced it.
- Strap slim (r3): same mechanism at the stern (see r3b above).
- contactZF 2.56: the flat run extended INTO the ref's ramp (2.60-col
  bottom 0.005 vs ref 0.069); 2.50 final.

LAW BANK (round discoveries, for BUILD-STANDARD folding):
- REGISTRATION-ANCHOR LAW (SSD addendum candidate): dAlong is the
  body-span midpoint delta — ANY edit that adds/removes a BODY column at
  either hull extreme (bow probe, stern straps) shifts the registration
  half a column and invalidates every authored side/turret target from
  prior digests. Fix the anchors FIRST, freeze them, then ladder.
  Body-column carriers at the extremes are LOAD-BEARING even when they
  look like dressing (the -3.425 strap face holds hullLengthM's rear).
- TEETER CO-LOCATION: when a ref staircase edge rides a col boundary
  (reads flip run-to-run), place MY edge at the ref's edge z so both
  flip together; when the exact ref edge is unmeasurable, split the
  difference and accept the phase EV.
- VERTEX-SCAN BLINDNESS: window-filtered vertex probes cannot see boxes
  that SPAN the window (corner verts outside) — AABB-based censuses are
  the attribution tool for spanning content; fitting tags
  (userData.fitting) survive merge and name the owner.
- Station topPct is procBox.min.y-coupled: any below-ground pad sag
  biases ALL 14 slices (+0.67% at -0.015) — padCornerFloor is the
  standing fix (centurion r6 lineage, first russia caller).
- ELLIPTICAL TIP COLLAR: muzzle furniture wider than the +-0.108 plan
  boundary goes in as an inscribed ellipse (y-half = side read, x-half
  <= 0.104) — the SSB3.1 inscribed-drum lane extended to gun tips.

HONEST RESIDUALS at 90.7 (worst first, all rows):
- side_whole -2.238 col: the ref bustle step edge teeters the -2.30
  boundary (1.74 vs 1.93 run-to-run); my step follows the r4 co-location
  + r7 margin discipline; worst-case phase costs ~0.077 on one col.
- Muzzle-zone plan/side center cols: my tube runs to the authored 6.20
  (ref print reads ~6.11) — the r27 spec-datum class, one col each row.
- Front ramp z 2.72-2.97: the ref ramp is CONVEX (slow-then-steep);
  the band tangent is straight — +-0.03-0.06 on three cols.
- turret_side cover 0.97: the turret-trim boundary (hull span + 0.6)
  lands mid-col and flips with grid phase — structural to the trim
  window, not authorable.
- Digest-frame ONLY-REF side col at ~3.46 (ref bow-nose content past my
  3.40 probe): pricing it costs dims + registration (see negatives).

SELF-READ TABLE (graduation severity, vs the oracle pairs at
shots/critic-t90m/ + the b5 yaw evidence; shape rows are gate-proven):
  view-left 9.1 | view-right 9.1 | view-front 9.0 | view-rear 9.0 |
  view-top 9.1 | frontleft 9.0 | frontright 9.0 | rearleft 9.0 |
  rearright 9.0 | hero-frontleft 9.0 | hero-rearright 9.0 |
  hero-toptilt 9.0 | close-front 9.1 | close-roof 9.0
Reads justified by: every gate component >=90 x2, dims 100 (width/height/
lengths inside grace), evaluator yawProxy <=1.1 all views, muzzle bore +
reference collar + boot mantlet grammar (SSB3.1 complete), RWS/kit
density mg1+4d, welded-turret identity tells (flat cheeks, asymmetric
shoulders + bustle, twin sight heads, course-stepped roof edge). Weakest
reads: view-front glacis ERA ribbing lighter than the print's bake;
skirt band reads more monolithic than the print's paneling at hero zoom.

**DUAL GATE CANDIDATE at 4acdebe4** — gate PASS x2 + self-reads >=9.0 on
all 14 views; graduation adjudication requested.

## r8 ORDER ROUND (2026-08-06, russia-family builder): the four graduation
## orders (the archived visual-review receipt) — candidate
## 4acdebe4 -> **e345ee8a** (49 meshes / 111241 verts)

GATE x2 at close (consecutive, identical to the decimal): **min 90.7 |
hull 90.7 whole 91.6 turret 93.4 stations 94.1 dims 100 floaters 100
PASS** — holds the certified 90.7 line (whole +0.3, turret +0.2,
stations -0.3 vs the graduation baseline). dAlong 1.363 / dy -0.003
UNMOVED both runs (REGISTRATION-ANCHOR discipline: bow probe 3.37 /
stern strap -3.425 body extremes untouched; per-column decode shows
every worst row inside its packet-priced family — teeter <=0.036,
front-ramp 0.03x, apron 0.051, no new class). npm test green (combat
253). Frozen sibs byte-identical at open AND close: pt91m 2cf10e23,
t72b3m 1e1ca4b8, t84 4707a9c (t72b3m also proves the eraRuCheeks rChev
guard — it exercises the relikt branch without the param).

ORDER 1 — WHEEL COUNTABILITY (§B8.1, geometry + tone):
- RENDER-DERIVED HEM MAP (fresh view-left pair, per-column calibrated
  scan; §D render-outranks-rows): the oracle's pale skirt hem is a
  CONSTANT 0.713 across the wheel run; below it there is NO curtain —
  the "mid tones" are the WHEELS' lit upper arcs (wheel columns read
  black 0..~0.40-0.59 then mid 0.43..0.70 then pale 0.713+); the
  verdict's flap-zone 0.68-0.74 onsets are the END windows. The r3
  "deep rubber hem" (full-length 0.454..0.78 grey band) was the wall:
  gate-honest content in the wrong visual form — the ref's priced
  ±1.85 front-col 0.453 bottom comes from its FLAP-ZONE hems only.
- EXECUTED: hem band deleted over the wheel run (kept: rear pair z
  -2.60..-1.83 at the priced 0.454 bottom — the ±1.85 col carrier —
  and a front taper pair at the ref's 0.52 line, the second chunk
  SHORTENED to z<=2.345 so the wheel-1 crown reads); skirt-band panels
  yBot 0.78 -> 0.713 (the pale hem lands ref-exact; the dark lip
  buries); bags + half-bags re-bucket 'hullTrack' -> 'hull' (the
  oracle's bags read PALE SCHEME with camo mottle — t72b3m rBucket
  precedent); road-wheel discs + tire rings swap to UNHOOKED dark
  clones (dish 0.44x scheme, tire 0x22201b — the ambient-floor hook
  itself pinned the old discs at the ~52 vertical-face floor, t72b3m
  r23 class; sprocket/idler bodies KEEP scheme steel per the ref's
  pale end windows); t72b3m-recipe wheel-face packages (rim arc at the
  0.330 dish seam, mid ring, hub drum + dark cap per wheel + idler/
  sprocket hub sets) in the per-side in-lane track buckets.
- DONE-GATE: six wheels countable at 1x BOTH sides (shots/critic-t90m
  view-left/right + shots/t90m-r8 crops); window luma p50: left 12 vs
  ref 16 (same black class), right 35 vs ref 19 (key-side, dark class,
  circles unambiguous); wheel-column dark-run tops 0.57-0.67 vs ref
  0.40-0.59; gap notches at y 0.13-0.30 read like the oracle's.
- track-clip --exact 0/0 band AND shoe, blind spots 0 (the wheel-1 rim
  arc crosses the idler-ramp shoe path in 3D by construction — the
  ring furniture lives in hullTrackDetailL/R + hullTrackTrimL/R, the
  §B4 designed in-lane dressing lane; 'hull'-bucket rings read 4/16
  at the front zone before the re-bucket). §B6 trapezoid untouched.

ORDER 2 — DRUM TONE (tone-only lane, DECODE SUPERSEDES THE ATTRIBUTION):
- The verdict's "warm TAN fuel drums dead-center at eye level" pixels
  are the UNDITCHING LOG (hullWood 0x6b543a rendered (117,94,67) at
  x ±0.86, y 1.15-1.32) — the actual drums at 1.66 never read tan.
  Three mechanisms measured: mats.detail retint (REJECTED — the
  'detail' slot is pattern-repaint-REGISTERED; a builder setHex never
  reached the render), camo 'hull' re-bucket (REJECTED — boxUV dropped
  a brown patch across the rack, byte-for-byte the same warm read),
  'hullCloth' re-bucket + per-tank canvasCloth green-shift (LANDED:
  0x39482e + 0x0a0d08 emissive floor — canvasCloth is UNREGISTERED so
  the retint holds; the warm key flips the default OD's up-facing
  lids tan from the top views).
- Drums + coupling + bins + LOG re-bucketed to the cloth slot (the
  oracle renders its whole rack GREEN — log zone samples (72,85,62) =
  its rear-plate family; a canvas-wrapped/OD-painted log is
  real-vehicle-plausible). Dark end rings + straps keep the
  strap-detail read. FLAG FOR THE CRITIC: the verdict's "log STAYS
  wood-tan" clause assumed the tan carrier was mats.detail — honoring
  it verbatim would leave the exact flagged pixels tan.
- DONE-GATE: flagged-zone sample PROC (69,86,58) luma 61/78/87 vs REF
  (72,85,62) luma 71/78/87 (p50 78==78, G-R +17 vs +13); warm census
  (R>G+12 & R>90) = 0 px in view-rear/top/rearleft/rearright/
  hero-rearright (was 5098/641/623/556).

ORDER 3 — CREW-HATCH RINGS (§B3/§B2, near-flush interior relief):
- The old commander drum (cylY at -0.45,0.66) was BURIED inside the
  hump volume (top 0.732 under the 0.8375 lid) — deleted. Two ring
  assemblies on the hump tops at the certified centers ±0.4825
  (commander z -0.50 — the T05BV-1 pedestal/slew ring sit inside the
  ring hole, riding the cupola like the real vehicle; gunner z -0.44),
  t72b3m cupola-redress grammar: ring wall cylY 0.262/0.272 + pale rim
  + lid + dark ring seam + hub + rear hinge + 5 periscope studs each.
- Ref-derived seats (view-top ring fits): cupola r~0.25-0.29 at
  x +0.54..0.63, gunner outer ring to r~0.41 at x -0.70. HONEST
  RESIDUAL: the ref gunner ring is wider than the hump band — mine
  hold r 0.27 interior to the certified rows rather than overhang the
  shoulder staircase.
- BUDGET: every top <=0.8495 local = world 2.2495 < the 2.2523 heightM
  1% grace (relief <=1.2 cm over the hump lid). dims 100 x2, stations
  i5/i7 at 0.18/0.71 topPct (unmoved class), plan rows unmoved
  (interior x/z). Rings render cloth-OD (uniform — camo 'turret' put a
  brown patch ON the rings; measured, moved).
- DONE-GATE: two bold circular hatch reads in view-top zoom +
  close-roof + hero-toptilt (shots/t90m-r8/proc-hatches-zoom3.png vs
  ref-hatches-zoom.png); §J yaw pair at e345ee8a: the rings sweep
  with the turret (shots/t90m-r8/yawpair-r8close-t90m.png).

ORDER 4 — RELIKT TILE-COURSE RELIEF (§B3 ERA grammar, mask-interior):
- eraRuCheeks rChev opt-in (defaults byte-identical — sibs prove it):
  per cassette 3 dark diagonal course seams + 2 pale crest strips at
  +0.8 mm proud of the calibrated face plane (sub-half-pixel, leopard
  r9 class; side raster 9.5 mm/px) + 3 course ribs on the tilted TOP
  shoulder (§B3.1 45°-shoulder free lane; rib crowns within +2 mm of
  the cassette's own certified corner envelope). Cassettes re-bucket
  turretTrack -> per-tank OD cloth (the oracle's chevron wedges read
  PALE with dark seams; steel sat a tone-class dark).
- Glacis wedges: 3 tile separators + 2 transverse course seams + upper
  crest line per wedge, nested in the wedge's own tilted frame (k5Seg
  mechanics) at +2.3 mm over the top face (sub-half-pixel vs the side
  raster; interior to front/plan columns).
- DONE-GATE: chevron courses read at 2-4x in view-frontleft /
  close-roof / view-front crops (shots/t90m-r8/proc-fl-cheek2.png,
  proc-close-cheek.png vs ref-close-cheek.png); gate held x2.

BATTERY at e345ee8a: track-clip --exact band 0/0 + shoe 0/0, blind
spots 0; winding-audit mode 1 rev 0 / mix 0 / FrontSide deficit 0px;
mode 2 HARD = the adjudicated rear drum-rack negative (candidatePx
total 1584 == the packet's 1330+161+93; content identical, mesh index
moved with the re-buckets — DON'T CHASE per the standing adjudication);
standard-check gateMin 90.7, clip 0/0, contig 0, decor mg1+4d;
visual-evaluator exit 0, RIG PARITY OK all 14 views (max yawProxy 1.1
deg @close-front, the baseline value); turret-parent stranded 1 =
"(unnamed)" merged-hull-bucket AABB false-flag (the packet's proven
pre-existing §B5 class, was 49% now 45% with the re-bucketed
composition); §J yaw pair rendered at the close hash.

CHANGED VIEWS: ALL 14 (order 1 moves the gear/skirt read in every
view; order 3 in all top/roof/quarter views; order 2 in the rear/top
family; order 4 in the front/quarter/close families). Full fresh
14-view adjudication required per the verdict.

LAW-BANK CANDIDATES (r8):
- REPAINT-REGISTERED SLOT RETINTS ARE DEAD CODE: builder-time setHex
  on wheels/wheelsDark/detail never survives to the render — the
  shared paintable registry rewrites them; per-tank tone work uses
  UNREGISTERED slots (canvasCloth/wood/rubber/dark/spareTrack) or
  unhooked clones. (Extends DETAIL-SLOT LOUD-CARRIER.)
- AMBIENT-FLOOR CEILING: vehicleAmbientFloorHook pins vertical faces
  at ~52L — any sub-40L vertical-face tone target requires an
  UNHOOKED clone (or the t72b3m r23 MeshBasicMaterial class); the
  pt91m rehook recipe is for >=45L targets only.
- CAMO-BUCKET PATCH LOTTERY: re-bucketing a small carrier to
  'hull'/'turret' samples ONE patch of the camo canvas at its
  position — a warm patch reproduces the exact defect being fixed;
  uniform-tone re-buckets take the cloth/steel slots.
- WARM-KEY UP-FACE FLIP: warm-balanced OD albedos read TAN on
  up-facing lids under the warm key even when correct on walls —
  sample top views before accepting any roof-furniture tone.
- ORACLE-RENDER HEM SEMANTICS (extends INTERIOR-READ TRIAD): a
  side-view "skirt wall" can be gate-priced content in the wrong
  visual FORM — decode the ref's below-hem zone per-column (black/
  mid/pale runs) before authoring any curtain; the priced front-col
  bottom may belong to the END zones only.

HONEST RESIDUALS (r8, beyond the packet's four priced classes — all
still inside their claims):
- Right-side wheel window reads p50 35 vs the ref's 19 (key-side
  lighting on the dish faces; circles unambiguous, left side 12/16).
- Gunner hatch ring r 0.27 vs the ref's ~0.41 read (hump-band
  interior constraint, above).
- Cheek chevron array reads lighter than the print's bake at hero
  zoom (relief present; the print bakes its courses into texture).
- Stations 94.4 -> 94.1 (the deleted mid-run hem chunks' slice widths;
  all stations <=1.42 wPct / <=1.42 topPct).

**RE-ADJUDICATION REQUESTED at e345ee8a** — all four orders executed
with measured done-gates; the orchestrator spawns the fresh 14-view
graduation critic.

## GRADUATED 2026-08-06 — DUAL-GATE PASS (fleet graduate 27)
Geo 90.7 gatePassed x2 EXACT (90.7/91.6/93.4/94.1/100/100) + independent
critic 9.0+ ALL FOURTEEN views on the second sitting (floor 9.0, mean
9.10; first sitting 8.4/8.76 — the mask-blind triad resolved with
measurements: wheels countable both sides at native tone, rack green
end-to-end warm-census-0, bold hatch rings + Relikt relief). FREEZE HASH
e345ee8a (49 meshes / 111241 verts, orchestrator-verified at landing).

The owner's dedicated-agent order ("make t-90m land perfectly")
COMPLETE: 81.7 -> 90.7 -> dual gate. Flip-era §10: no runtime
registration (dump clean), mirrors x3, variants rows benign, icons x5
from a clean HEAD worktree. CERTIFIED-CLASS ANNOTATION: winding mode-2
1584px = the rear drum-rack (rig_hull z -3.42..-2.92) — hull-correct by
adjudication x3 (bustle overhang class). Discipline note: the r8 packet
line "all stations <=1.42 wPct" was wrong (i9 reads 5.45; the component
number 94.1 itself was honest).

## Native-only Proryv replacement (2026-08-12)

The owner rejected the earlier mask-shaped result as too slabby in its hull,
turret and running-gear presentation. The active playable now calls
`buildT90MProryvNative2026`: a repository-authored low V-bow hull, independent
welded fighting compartment, planted Relikt, supported tapered bustle/rack and
one exact native six-wheel linked course. The comparison GLB is a visual and
measurement oracle only; no source mesh, vertex array, material, texture, rig
or converted payload enters runtime.

Freeze **`a21894b8`** reproduces at 55 meshes / 114,746 vertices. Independent
raster fidelity is **90.96 aggregate**, **90.02 minimum required view**,
**92.73 whole silhouette**, **91.67 hull** and **91.25 tracks**. The direct
turret diagnostic is 86.86 because the comparison hierarchy assigns only the
forward tube to `Main_barrel`; its sleeve/root remain turret siblings. All 14
complete-vehicle views pass the acceptance law.

The legacy geometry-gate row remains an honest **27.1 FAIL**. Its contour and
component masks were calibrated to the retired slab-shaped implementation and
are incompatible with the accepted native model/source split. This red row is
preserved and documented rather than used to push authored geometry back
toward the comparison asset.

The 42 distinct frames in
`/tmp/critic-t90m-proryv-native-final-r4/t90m` prove genuine quarter-turn yaw.
Gun/mantlet, complete shell, Relikt, roof stations, bustle/rack and strapped
transverse cylinder rotate together; V-bow, skirts, wheels/course, engine deck,
transom and rear hull drums stay fixed. Exact band/shoe containment is 0/0 at
both terminals, turret-parent audit is clean, winding is 0 reversed / 0 mixed,
and the open muzzle probe passes. **RE-FROZEN / KEEP `a21894b8`; retire
`e345ee8a`.**
