# T-72B3 (`t72b3`) — oracle packet

Spec home: src/vehicles/modern1.ts (dims 6.67 / 9.53 / 3.59 / 2.23).
Build: buildT72B3 (modern1) — pre-oracle-era procedural, no photo-class
round yet. Family guidance (owner 2026-08-06): t72b3 takes inspiration
from the t72 family.

## ORACLE ONBOARDED (2026-08-06 base-21 wave)
"T-72B3M Obr. 2022" by 42manako (4th verified drop from this author:
bradley, fv510, puma), CC-BY-4.0 verified live —
`community/t-72b3m_obr._2022.glb` (54 MB). Registered for the t72b3
BASE id ONLY in all THREE harness maps + vertex REG: turretNode
`^Object_14$` (turret shell + K-1/soft-case ERA; its mesh includes
gun-run cladding, so the turret mask reaches toward the muzzle),
turretFollowers `^Object_3$` (tall right-roof cluster, y 1.51..2.95),
gunNode `^Object_15$` (5.6 m tube), yaw PI (raw muzzle -z). Extract:
docs/references/vertex/t72b3.json. The t72b3m GRADUATE registration
(recovered/t72b3m.glb, `^misc_a$`/`^misc_b$`) is UNTOUCHED.

### Print facts
- Dims after yaw PI: hull mask 6.813 (+2.1% — rear log/drums at the
  stern inflate it, the t72b_1987 drum-band class), overall 9.315
  (-2.3%), width 3.589 (0%). BODY HEIGHT 3.276 = +46.9% vs the 2.23
  datum: the obr-2022 roof cluster (Object_3) carries the p95 — a §E
  normalize (roof-cluster datum treatment) is REQUIRED before curve
  rows can measure the real vehicle. §E height clamp binds (s 0.6678);
  width safeScale k 1.3606 recovers the frame (net -1.6% class).
- ORIENTATION-ASSERT ARTIFACT (law note): glacis vote reads -z because
  Object_3 (a turret follower this tool partitions into HULL — split
  modeling limitation) tops the hull curve at z ~0.08 with 3.41, and
  the stern log sits high (1.61 at z -4.53). Deck evidence (nose tip
  1.03 at z +2.28, glacis run at +z) adjudicates nose=+z; the print is
  correctly oriented. INTERPEN 1933 verts / 1.90 m worst (basket
  content in the turret node).

### HONEST BASELINE (single-id gate x2 at HEAD, 2026-08-06 — first t72b3 ledger rows ever)
geoMin 0 x2: hull 0, whole 0, turret 0, stations 0, dims 74.1, floaters
100 (a pre-15a67ea run read hull 1.3 — the belly-pan revert moved the
proc hull mask; post-revert stable). dims 74.1 = the OLD proc build
(height 2.16 -3.02%, hull 6.52 -2.22%) — the expected low honest row
for a pre-oracle build. Worst columns: side_whole z 0.79 (refTop 1.70
vs procTop 0.32, err 0.82 — the print's turret+roof cluster vs the old
low turret), front_whole z -1.03 (err 1.07), turret_plan z 0.18 (err
1.59), plan rows healthiest (76-81). Work order: this is a FULL
photo-class family round (t72 grammar), not a tweak list.

## t72b3m GRADUATE RE-ORACLE COMPARISON (evidence only — decision = orchestrator lane)
The two prints are the SAME AUTHOR'S MODEL, two revisions apart:
recovered/t72b3m.glb carries the identical material set (chassis.0/1,
dz_l.1, dz_r.1, k-1.0, misc_a.1/2/3), identical raw width 3.9510, and
a mesh literally named `T-72B3M_Zavod_edition` (Blender export;
"recovered fleet — authors not preserved" can likely be upgraded to
42manako attribution if the owner verifies their catalog).

| axis | recovered/t72b3m.glb (graduate's oracle) | t-72b3m_obr._2022.glb (new drop) |
|---|---|---|
| verts/tris | 152,693 / 119,993 | 156,371 / 130,716 (+10.7k tris) |
| dims vs published | height +0.3%, hull -0.2%, overall 0%, width 0% (NORMALIZED-EXACT) | height +46.9% (obr-2022 roof cluster), hull +2.1%, overall -2.3% |
| split | authored misc_a/misc_b, clean (interpen 209 / 0.40 m) | flat Object_N, follower needed, gun-run cladding in the turret mesh (interpen 1933 / 1.90 m) |
| orientation assert | AGREES | artifact-mismatch (adjudicated OK, see above) |
| configuration | B3M baseline | B3M obr. 2022 (roof cluster, updated fit) |
| graduate row | frozen 91.8 PASS against THIS print | would re-baseline low until a §E normalize lands |

RECOMMENDATION: DO NOT swap the graduate's oracle now. The recovered
print is dims-exact, split-clean, and the frozen 91.8 row certifies
against it; the new print needs a §E roof-cluster normalize + follower
split work, and it depicts a LATER configuration whose furniture the
graduate build does not model (a swap is a feature order, not a
measurement improvement). The new print's value today: (a) the t72b3
BASE oracle (landed, this packet); (b) obr-2022 feature/density
reference for a future t72b3m round — price the re-oracle THERE if the
owner wants the 2022 configuration certified (full re-gate + re-cert
per the queue entry).

## 2026-08-06 FLEET MUZZLE-BORE + §C.1 WINDING SWEEP (fleet-sweep one-liner)
- §B3.1 bore on the 2A46M-5 tube (len 6.0); mantlet: embrasure block+collar present - russian BOOT grammar upgrade noted for its queued base-21 round; §C.1 0 reversed; F-vs-D 0; gate HELD x2 EXACT (0-curves row pre-existing; base-oracle onboarding queued); hash not frozen; mantlet mass verified per MANTLETS-MANDATORY (db9168c). Mechanism: kit.js muzzleBore shadow-named furniture + orientedSlab guard (3fca39b / 1017339); end-on+quarter crops shots/muzzle-sweep/{before,after}/.

## BASE-ID RETIRED / ORACLE PROMOTED (2026-08-06)

The owner delisted t72b3 from the roster (§4.997) and overrode t72b3m's
oracle to this print (§4.998). The base-id registrations this packet
created moved to the t72b3m key in all four maps; docs/references/
vertex/t72b3.json was deleted (stale pre-batch-45 bytes of a delisted
id — t72b3m.json, regenerated post-warp, is the living extract). The
no-swap RECOMMENDATION above is superseded by the owner override; the
comparison evidence stands as the swap's engineering basis.
