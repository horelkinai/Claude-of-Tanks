# t84 — T-84 Oplot (Ukraine) — GRADUATE packet

(Created retroactively 2026-08-07 by the orchestrator: the §5.03
reversed-core sweep found this graduate had NO packet at all. This
file backfills the registry facts and adjudication state; the tank's
detailed round history predates the packet convention.)

## Registry facts (PROGRAM-STATE §3)
- **GRADUATE** (the 19th, 2026-08-04), frozen hash **04707a9c**
  (43 meshes / 85,876 verts via tmp-hashgeo, camoSeed 4242).
- Gate line (current, tool-written docs/geometry-gate/t84.json):
  **92.5 | hull 92.5 / whole 93.2 / turret 93.2 / stations 96.1 /
  dims 99.1 / floaters 100** — dual-gate PASS.
- Builder: `buildT84` in `src/vehicles/profiles/russia.ts` (Ukraine id
  in the russia family file). Registration retired per §10 at
  graduation — the recovered print measures via the three override
  maps (procedural-fidelity / visual-evaluator / tmp-tank-critic:
  `recovered/t84.glb`, turretNode `^Turret$`, autoPivot,
  paintUntextured; "RIG PARITY OK 0.8-1.8deg across all r31/r32 runs"
  per the map comment).
- Oracle repair history: **batch-40 compound warp** (turret seat) in
  tools/repair_oracles.py — the "python asserts must target the LAST
  op of a chain" law (PROGRAM-STATE §4.9x) was banked from its
  double-warp incident.

## §C.1 adjudication state
- **LATENT reversed right-turret core (§5.03 sweep item 6,
  2026-08-07):** rig_turret mesh#37, 12 tris, signed vol −0.092 m³,
  world AABB [0.86, 1.34, −0.16] → [1.22, 1.67, 0.85] — a 1.01 m ×
  0.32 m solid inside the right turret band. **Deficit 0 today (fully
  occluded)** — per the §C.1 latent class it is FLAGGED-UNTIL-VISIBLE,
  not an order. Any fix rides the GRADUATE-CHANGE protocol (fix →
  gate hold ×2 → independent re-cert critic on changed views →
  re-freeze the new hash, ONE commit).
- Mode-2: not on the sweep's candidate list (clean).
- Fleet notes touching t84: procShadow_gun oversize true-up candidate
  (family-lane follow-up list); "near-blind front 162" clip-cell count
  in the certified backlog table.

## Standing orders
None open. The tank is a stable graduate; do not edit buildT84 outside
the graduate-change protocol, and never move its geometry in a shared-
helper change without hash-verifying 04707a9c before and after.
